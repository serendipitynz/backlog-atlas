---
id: TASK-145
title: 窓が狭いとき、固定見出しがタスク詳細の下部区画を押せなくするのを直す
status: Done
assignee: []
created_date: '2026-08-10 20:49'
updated_date: '2026-08-18 21:09'
labels:
  - 'kind:bug'
milestone: m-2
dependencies: []
priority: high
ordinal: 141500
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
TASK-137 の実測（2026-08-11、借り物 playwright・WebKit）で見つけた。編集セッションを開いた状態でタスク詳細パネルを最下部までスクロールすると、**外部エディタで開く 区画の控えの中心で `elementFromPoint` が見出しの `dl` を返し、控えが押せない**。窓 560×420 で再現し、**700×420・900×520・1280×480・1280×600・1280×800 では 5 条件とも控え自身を返す（押せる）** ので、効いているのは高さではなく**幅**である。既定の窓は 1200×800（`tauri.conf.json`）で最小寸法の指定は無いため、この幅は到達しうる。

**TASK-137 が作った欠陥ではない** — 固定見出しは TASK-72、固定した発行の行は TASK-135 が入れたもので、TASK-137 が動かしたのは控えの語 1 文字ぶんである。狭い窓では併置のパネル幅が `min(30rem, 45vw)` で 252px まで縮み、**見出しが折り返して高さを食い尽くす**のが原因と見ているが、これは推測なので着手時に測って確かめる。

**公開阻害としてこちらが判断した** — 提供している控えが操作不能になる。用語節の定義により、発生条件の限定（狭い窓に限る）はこの判定に入れない。

**着手時に確かめること**: 覆っているのが見出しか発行の行か（両方とも固定されている）、押せなくなる幅の境目、同じことが他の最下部の区画（Git 履歴欄）でも起きるか、doc-8 §2.2 の見出しの固定と doc-11 §11 の発行の行の固定のどちらに上限を与えるべきか。
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 窓を狭くした状態でも、タスク詳細の最下部の区画の控えが押せる（`elementFromPoint` が控え自身を返す）
- [x] #2 押せなくなっていた幅の境目と、覆っていた要素が実測で特定され、Implementation Notes にある
- [x] #3 固定される行の高さの上限をどちらの doc が持つかが決まり、その doc に書かれている
- [x] #4 同型（最下部の区画が固定行に覆われる）が他に無いことを数え、結果が Implementation Notes にある
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## 起票の記述と実測が 3 点違った

**測ったもの**: `_sandbox/app-check/`（本物の `App.svelte` ＋ 本物の `commands.ts` ＋ 偽 IPC）を WebKit・Chromium の両方で、窓 25 条件 × 閲覧／編集セッション。手順は `_sandbox/task-145/measure-band.mjs`・`measure-reach.mjs`・`measure-siblings.mjs`、値は同ディレクトリの `*.json`。

**1. 編集セッションは要らなかった。** 560×420 の閲覧でも同じ覆いが出る。**閲覧のほうが見出しは高い**（198.22px 対 166.67px、Chromium）— 閲覧の status 欄は札が折り返し、編集中は `<select>` が 1 行に収まるためである。

**2. 「操作不能」は 560×420 では起きていなかった。** 起票が観測したのは**最大スクロール位置で控えが覆われること**で、これは固定表示の帰結そのものであり **1280×800 でも起きている**（`全面表示で開く →` が見出しの下に入る）。**公開阻害に当たるのは到達不能** — 面をどのスクロール位置へ送っても控えの中心が最前面にならない状態 — のほうで、それが現に起きていたのは **560×300（帯 −7.29px／控え 26 件）と 360×420・320×420 の閲覧（帯 −20.91・−37.72px／控え 12 件）** である。2 つは別々に立つので対応表（`_sandbox/handoff/referent-table/referent-table-task-145.md` 第 1 版）で語を分けた。

**3. 「効いているのは高さではなく幅」も誤り。** 幅 560 のまま高さだけ 420 → 300 に下げると到達不能になる。効いているのは**見出しの高さ ÷ パネルの clientHeight** で、帯は 1280×800 の 82.28% から単調に落ちる（560×420 で 37.5%、560×300 で −3.7%）。起票の 6 標本は高さが 420〜800 の広い幅ばかりで、比が動く方向を 1 つしか見ていなかった。

## AC #2 覆っていた要素と境目

**覆っていたのは見出しで、発行の行ではない。** 100 条件のどれでも `.issue` は覆う側に出なかった（下端 38.4px のまま育たない）。`elementFromPoint` が返したのは `header.heading`・`dl.facts`・`dt`・`dd` で、いずれも見出しの中である。

**見出しの内訳**（WebKit・閲覧、1280 / 560 / 320px 窓）: 1 行目 22.72 / 50.38 / 77.56px、2 行目 23.19 / 23.19 / 40px、**属性表 55.78 / 94.58 / 162.58px**。**育つのは属性表と 1 行目で、2 行目はほぼ動かない。**

**境目は幅の 1 点では書けない。** 帯が 0 を割るのは (a) 幅 560 のまま高さ 300（clientHeight 197px・閲覧で帯 −1.22px）と (b) 高さ 420 のまま幅 360（clientHeight 269px・閲覧で帯 −27.67px）。**閲覧のほうが先に割れる**（編集は `<select>` で属性表が縮むため）。**帯が正でも控えの高さ 22.45px に満たなければ到達不能は起きる** — WebKit 560×300 の閲覧は帯 2.28px で 5 件が到達不能だった。

## AC #3 上限は doc-11 が持つ

**doc-11 §13「固定行の高さの上限」を新設した。** 争点として提示し、ユーザーが確定した（3 点とも推奨案）。doc-8 §2.2 でも doc-11 §11 でもなく doc-11 に新しい節を立てたのは、**覆う側が「見出し」と「発行の行」の 2 種に分かれていて、置き場を定める節が doc-8 §3 と doc-11 §11 に離れている**一方、覆う側としては 1 つの型だからである。**doc-11 §11 に置いても今回の欠陥には掛からない**（発行の行は 1 条件も覆っていない）。

**規則は「覆われない帯は、その面に立つ控えの高さ（doc-11 §2.2 の段階の最大値 1.75rem）を下回らない」。** **割合で書かなかったのは、割合では守れない窓があるためである** — 560×300 の編集セッションで帯は面の 32.2% になるが、そこでも到達不能は 0 件である。「面の半分」のような規則にすると、規則の側が実装より先に破れる。

## 直したもの

**① 属性表を固定帯の外へ出した**（doc-8 §3 を 3 行の固定から 2 行の固定へ改訂）。**doc-8 §3 が固定の理由に挙げる 3 つ（ID コピー・前後移動・閉じる）はいずれも 1 行目の控えで、属性表にはその理由が及んでいない。** 画面設計案 02 は 3 行を固定で描いているので意図的な逸脱にあたり、doc-12 §3 と逸脱表に記録した。

**② 見出し 1 行目の折り返しを、ID の下限（4rem）付きの折り返しへ改めた**（doc-8 §2.2 の「1 行に収める」を条件付きへ）。**着手して分かったのは、この行には floor があって「1 行に収める」が成り立たない幅があること** — 控えの群が 253.73px（WebKit）/ 257.14px（Chromium）を占めて縮まず、併置サイドバーは `min(30rem, 45vw)` なので窓 590px あたりで内容幅がその数を切る。`nowrap` で押し通すと **560px 窓で 12 / 16px の横あふれ**になるか、**600 / 620px 窓で横断タスクID の描画幅が 0** になる（どちらも実測）。後者は doc-8 §2.2 が ID に与えた役目を画面から消すので、下限を与えて折り返しを許す形をユーザーが選んだ。**1 行に収めることは目的ではなく帯を守る手段だった**ので、①で帯が守られた以上この行を押し通す理由は無い。

**③ 属性表の `select` に `min-width: 0` を与えた**（横あふれ）。**本タスクの変更前から在る欠陥で、私の変更が作ったものではない** — 変更前実測で編集セッション中に 700px 窓で 35px、560px で 66px、480px で 84px あふれていた。`minmax(0, 1fr)` はトラックを 0 まで許すが、その中の `select` が自分の `min-width: auto` を保っていた。移した当の要素なので同じ PR で閉じた。**あわせて、その場の註が書いていた「The whole table drops to one pair per line under a narrow box」が誤りであることも記録した**（4 トラックに折り返しの規則は無い）。

**④ doc-8 の参照ずれを 2 か所直した。** §2 の割当と §3 の区画見出しの項が、どちらも見出しの固定を「§2.2」に帰していたが、**§2.2 が定めるのは切替と既定で、固定を述べているのは §3 である**。

**⑤ `.title-line` の註の事実を直した。**「The row is one line in both states (目視 2026-08-04)」と書かれていたが、320px 窓では 40px（2 行）である。目視が広い窓で行われていた。

## AC #1・#4 変更後の実測

**AC #1: 到達不能は 0 件。** 数えたのは **窓 11 条件 × 閲覧・編集 = 22 条件**を **2 エンジン**で回した **44 回**である（`band-after.json` の行数がこの 44）。横あふれも 0。帯は次のとおり（Chromium）:

| 窓 | 変更前の帯（閲覧／編集） | 変更後の帯（閲覧／編集） |
|---|---|---|
| 1280×800 | 593.92px 82.0% ／ 546.5px 75.5% | 656.8px 90.7% ／ 618.4px 85.4% |
| 560×420 | 118.78px 37.5% ／ 111.69px 35.2% | 221.98px 70.0% ／ 183.48px 57.9% |
| 560×300 | **−1.22px** ／ **−8.31px** | 101.98px 51.8% ／ **63.48px** 32.2% |
| 320×420 | **−45.84px** ／ 36.3px | 128.42px 47.7% ／ 108.06px 40.2% |

**最小値 63.48px は 1.75rem（28px）の 2.3 倍**で、doc-11 §13 の規則を満たす。

**AC #4: 同型は 0 件ではなく 6 面あった。** 面を名指しで列挙せず、DOM を走査して「スクロールする箱」と「その中の `position: sticky` な子」を computed style から拾って数えた（`measure-siblings.mjs`）。**覆いは足し算では出せない** — スイムレーンの列ヘッダ 5 枚は横に並んだ 1 行、レーンヘッダは行ごとに 1 枚で同時に貼り付くのは 1 枚なので、最大スクロール位置で固定行が現に占める y 区間の和集合を取った。

| 面 | 固定行 | 帯（1280×800） | 帯（560×300） |
|---|---|---|---|
| タスク詳細（閲覧） | 見出し | 656.8px 90.7% | 101.98px 51.8% |
| タスク詳細（編集） | 見出し ＋ 発行の行 | 618.4px 85.4% | 63.48px 32.2% |
| スイムレーン | 列ヘッダ行 ＋ レーンヘッダ行 | 691.0px 95.5% | 164.03px 83.3% |
| 設定モーダル | 見出し ＋ 下部操作行 | 661.9px 90.2% | 161.91px 69.2% |
| プロジェクト登録モーダル | 発行の行 | 223.6px 84.4% | 192.17px 82.1% |
| プロジェクト詳細の文書ペイン（編集） | 発行の行 | 665.4px | 71.27px |

**育つのはタスク詳細の見出しだけではない。** プロジェクト詳細の文書ペインの発行の行は無効化の理由が折り返して **41.56 → 105.73px** になり、560×300 では 177px の箱の 6 割を取る（帯 71.27px なので到達不能ではない）。**スイムレーンの 2 層は 1 行に保たれていて育たない** — `Swimlane.svelte` が `nowrap` と省略記号で明示的にそうしている。**現に規則を割っていたのは 1 面だけだが、同じ作りの面は 6 つある**というのが数え上げの結果である。

## 測っていないもの（目視へ回す範囲）

- **カーソル・ホバー・フォーカスの見え方**は数値で閉じられない。
- **属性表が固定帯から出たことの読み心地** — 1280×800 でも本文を送ると status・priority が画面から消える。数値では「帯が広がった」しか言えない。
- **見出し 1 行目が 2 行に折り返したときの並び**（1 行目に ID・コピー・印・前後移動、2 行目に配置切替・閉じる）。折り返した 2 行目が左端から始まることの見え方。
- **実機の webview の描画差**（TASK-74 のフォーカスリング切れのように、playwright の WebKit では再現しない差がある）。

## 試験

**自動試験は足していない。** 帯も覆いもレイアウトの値で、`jsdom` はレイアウトを走らせず `render.ts` の矩形は検査してはいけない値である（AGENTS.md）。**画面横断契約としても取れない** — doc-11 §13 が述べるのは幾何であって、DOM の形でも呼び出し順でもない。押さえているのは実測（`_sandbox/task-145/`）である。`pnpm test` 808 件・`pnpm run check` 0 errors・`cargo fmt --check`・`cargo clippy -D warnings` はいずれも通っている。

## レビュー 1R の反映

**[P2] 上限を書いただけでは、規則が掛かる範囲が閉じていなかった。** 指摘は成立し、**実測は指摘より
広かった** — 固定帯に置いた title には長さの上限が無く、**480 字で 640×480 の窓の帯が −126.0px、
パネルの控え 12 件すべてが到達不能**になる（両エンジン）。窓の最小寸法だけでは閉じない欠陥である。

| 窓 | title 20 字 | 60 字 | 120 字 | 240 字 | 480 字 |
|---|---|---|---|---|---|
| 1280×800 | 656.8 | 638.6 | 618.1 | 556.6 | 433.5 |
| 640×480 | 263.8 | 243.3 | 181.8 | 79.2 | **−126.0** |
| 560×420 | 203.8 | 162.8 | 101.2 | **−21.9** | **−309.1** |
| 560×300 | 83.8 | 42.8 | **−18.8** | **−141.9** | **−429.1** |

（Chromium の帯 px。太字は到達不能 12 / 12 件。台帳に長い title が無いので、描かれた文字列を
playwright から差し替えて測った — TASK-130 と同じ型）

**直したのは 2 つ。**（a）**title を 2 行で止め**、落とした分は `title` 属性が持つ。止めた後は
**2,000 字でも固定行の高さが動かない**（WebKit 112.16px・Chromium 113.19px。両エンジン・全窓）。
（b）**`tauri.conf.json` に最小窓寸法 640×480 を入れた**（ユーザーが 3 案から確定）。

**(b) が要るのは、上限を持つ固定行だけにしても面の高さが足りなければ帯が控えの高さに届かないから
である。** title を止めた後の境目を測ると、**規則を割る条件は高さだけで決まり**、幅 1280px でも
高さ 200px なら帯は 18.63px になる。**高さ 360px 以上ならどの幅でも成り立つ**（幅 320px を含む）ので、
640×480 はその境目に余裕を取った値である。640×480 の帯は閲覧 263.8px・編集 243.8px で、規則の 8.7 倍。

| 窓の高さ | 200 | 240 | 280 | 300 | 320 | 360 | 400 以上 |
|---|---|---|---|---|---|---|---|
| 帯 ≥ 28px | 全幅で× | 幅 800 以上のみ○ | 幅 480 以上○ | 幅 400 以上○ | 幅 480 以上○ | **全幅で○** | 全幅で○ |

**doc の側も 2 か所直した。** doc-11 §13 は「アプリの窓に最小寸法の指定は無い」と書いていたので、
最小寸法と、それが規則の範囲を作ることへ書き換えた。**「外へ出せないものは固定行の中で長さを止める」
を §13 に足した** — §11 が固定行の中に置くことを求めているもの（無効化の理由・発行のキーの併記）と、
各画面が固定行に置くと定めた値（title）は、外へ出す道が無いためである。doc-8 §3 の側は、2 行目が
2 行までしか伸びないことを**止めているから成り立つ**と書き直した（改訂前の記述は fixture の短い
title でしか成り立っていなかった）。

**[P3] 数え方が合っていなかった。**「22 条件」と書いたが、内訳に「両エンジン ×」を含めていた —
正しくは **窓 11 条件 × 閲覧・編集 = 22 条件を 2 エンジンで回した 44 回**である。上の AC #1 の行を直した。

**手順は `_sandbox/task-145/measure-longtitle.mjs`・`measure-boundary.mjs`。**

## レビュー 2R の反映

**[P2] 同じ形が発行の行の側にもあった。** 指摘は成立する。`manage.ts` の `commaReason` は、利用者が
打ったラベル・タグを**丸ごと**理由文へ埋めており、その理由文は固定行（doc-11 §11）の中に上限なしで
描かれる。**実測（プロジェクト詳細の新規タスク、窓 640×480）**: ラベル 10 字で固定行 81.5px（帯
285.5px）、200 字で 165.48px（帯 201.52px）、**600 字で 350.25px（帯 16.75px）** — 367px の面に対して
規則の 28px を割る。

**1R で数え落としていた。** 1R の対応で title を止めたとき、**同じ「固定行に入る上限を持たない値」を
全数え上げしていれば同じ回で出ていた** — 数えたのは固定行の側（6 面）であって、そこに入る値の側では
なかった。

**止め方は title と違う。** **描画で行数を切ってはならない** — doc-11 §5 が求めているのはホバーせずに
理由が読めることで、行数で止めると半分が `title` へ移る。**文の側に上限を持たせた**: `commaReason` は
値を頭 20 字＋省略記号で引用する（引用の役目はどの値かを言うことなので頭で足りる）。**止めた後は
600 字でも固定行は 81.5px で動かない**（両エンジン）。

**doc-11 §13 の当該項を 2 通りに分けた** — 落としても役目が残る値は行数で止めて全体を `title` が持つ
（title）、押す前に読めなければならない文は文の側に上限を持たせる（§5 の無効化の理由）。**どちらを
取るかは §5 が決める。**

**試験を 1 件足した**（`manage.test.ts`）。**これは純関数で押さえられる** — 帯や覆いと違って
レイアウトを要らないためで、理由文が長いラベルを丸ごと含まないこと・どのラベルかは頭で分かること・
文の長さに上限があることを見る。`pnpm test` は 809 件になった。

**手順は `_sandbox/task-145/measure-reason.mjs`。**

## レビュー 3R の反映

**[P3] 引用の切り方が文字の途中に落ちうる。** 指摘は成立する。`String.prototype.slice` が切るのは
UTF-16 のコード単位なので、**絵文字のように 1 文字が 2 単位のものを半分で切ると、利用者が打っていない
`�` が出る** — 打った値を名指しするのが役目の文で、その値を偽って写すことになる。19 字＋絵文字＋
カンマで再現した。

**コードポイントで切る形に替えた**（`[...value]`）。**書記素クラスタでは切っていない** — ZWJ で
つながった絵文字列は部品に分かれる。**それを許すのは、`�` と違って「どの値か」は分かるからである。**
`Intl.Segmenter` を使えば書記素で切れるが、**3 OS の webview すべてで使えることをこの環境では
確かめられない**ので、確かめずに依存しない。

**試験を 1 件足し、壊して落ちることを確かめた**（コードポイント版を `slice` へ戻すと当該 1 件だけが
落ちる）。**長さの検査もコードポイントで数える形へ直した** — `length` は UTF-16 単位を数えるので、
全部が 2 単位の値だと描かれる幅が変わらないまま数だけ倍になる。`pnpm test` は 810 件になった。

## 目視の結果

**2026-08-11 に目視 OK。指摘 0 件・追加コミットなし。** 回した範囲は、数値で閉じられなかった 4 点
（カーソル・ホバー・フォーカスの見え方、属性表が固定帯から出た後の読み心地、見出し 1 行目が
2 行に折り返したときの並び、実機 webview の描画差）である。

**「属性表が 1280×800 でも本文を送ると画面から消える」ことは許容された** — これは固定帯から外した
ことの直接の帰結で、doc-8 §3 の改訂と画面設計案 02 からの意図的な逸脱として記録してある。
<!-- SECTION:NOTES:END -->
