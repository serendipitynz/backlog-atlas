---
id: TASK-76
title: モーダルの閉じる操作を右上の × に統一する
status: Done
assignee: []
created_date: '2026-07-31 23:31'
updated_date: '2026-08-05 01:17'
labels:
  - ui
  - design-system
  - 'kind:bug'
milestone: m-2
dependencies:
  - TASK-74
documentation:
  - backlog/docs/doc-11 - 画面共通のデザインシステム-設計.md
priority: high
ordinal: 76000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
モーダルの閉じるボタンの位置が統一されていない。共通の Modal コンポーネントが閉じる操作を持たず、各利用側が下部や見出し横に「閉じる」というテキストボタンを置いているためである。Modal 自身が右上の × を持ち、全モーダルがそれを使う形にする。文言ではなく × とし、aria-label で用途を示す。設定モーダルの下部固定ボタンとは役割が違う（× は破棄側）ので、両者の関係を doc-11 に書く。
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Modal が右上の × を持ち、全モーダルが同じ位置・同じ見た目で閉じられる
- [x] #2 × に aria-label があり、キーボードから到達できる
- [x] #3 × と設定モーダルの「変更せずに閉じる」の役割の別が doc-11 に書かれている
- [x] #4 各利用側から独自の「閉じる」テキストボタンが消えている（設定モーダル下部の「変更せずに閉じる」は TASK-74 が置く保存対の操作なので対象外）
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
<!-- SECTION:NOTES:BEGIN -->
## 決定先行の判定（決 印は後付け・11 例目）

着手時に「契約を変えるか」を当たった結果、**doc-11 に新しい節が要る**と分かった。AC #3 が
求める「× と『変更せずに閉じる』の役割の別」を書く場所が doc-11 に無く（モーダルの規則は
doc-7 §2.1 が持っていた）、新しい doc の節を足すのも契約の変更である。decision は書いていない
（doc 改訂だけで足りる 10 例目）。

**材料を先に確かめてから聞いた**（TASK-113・114 の手順）。doc-12 §2.2 の転記は原文の設定
モーダルを「ボタンは『閉じる』だけ」としか書いておらず、隅の × を控えとして数えたかどうかが
判別できなかったので、原文の図を求めた。ユーザーが図を提示し、**原文には右上の × と下部左端の
「閉じる」が両方あり、保存ボタンが無い**（＝値の変更が即時に効く前提）ことが分かった。**この 2 つは
原文では同じ操作**である。転記はこの × を落としていたので doc-12 §2.2 を訂正した。

**ユーザーが確定させたのは 1 点**: 設定モーダルの出口が ×・変更せずに閉じる・Escape の 3 つに
なることを許容する（理想は設定だけ例外だが、AC 整合と後続タスクを優先して 3 つとも置く）。
原文にも × と下部の控えが両方あるので、3 つ置く形のほうが原文に近い。

## 範囲（AC #1「全モーダル」）

`Modal.svelte` の 3 つ（プロジェクト登録・設定・キーボード操作の一覧）に限る。Description が
原因を「共通の Modal コンポーネントが閉じる操作を持たず」と名指しているため。**doc-8 §2.1 の
中央モーダル詳細配置は含めない** — doc-8 §2.2・§3 と doc-12 §3 は既に見出しの閉じるを
「閉じる ×」と書いており実装は文言のままなので差分は実在するが、**全面シングルビューの原文は
「← 戻る」で × ではない**一方 doc-8 §3 は見出し 1 行目を 3 配置共通としている。ここを触ると
3 配置の見出しの契約を開け直すことになるので TASK-80 へ送る。

## 変えたもの

- **doc-11 に §7「モーダルの閉じる」を新設**し、旧 §7（SCSS への写し方）・旧 §8（後続への影響）を
  §8・§9 へ送った。参照は先に grep して 2 件（`src/app.scss` と TASK-71 の Implementation Notes）。
  前者は書き換え、後者は Done タスクの記録なので触らず、doc-11 §9 に「TASK-71 が指す §8 は
  現在の §9」と読み替えを書いた。
- **doc-12 §2.2 を訂正**（原文に右上の × がある）。§9 の引用元一覧に doc-11 の 01 と TASK-76 の行を足した。
- **doc-7 §2.1** に、押して閉じる操作の形は doc-11 §7 が定める旨の 1 文。
- **`src/lib/icons/lucide.ts` に lucide の `x`**（lucide-react v1.17.0 の `__iconNode` を実物で確認。
  `path` 2 本なので要素種別の列挙は増えない）。**doc-11 §2.4 の型は増えていない** — 図形にしたので
  「アイコンのみのボタン」にそのまま入る。**既存の文字グリフ `×` 3 か所**（上部帯通知・絞り込み
  トークン・status 別名表の行削除）は本節より古いので据え置き、doc-11 §7 に反例として明記した
  （TASK-80 が扱う）。
- **`Modal.svelte` が × と `closeBlocked` を持つ**。閉じられない事情はシェルが持ち、prop で降ろす —
  Escape と下部操作行の押下も同じ 1 つの事情に従う必要があり、× だけが自分で判断すると 3 つの
  出口が別々の事実を見る。× は `aria-disabled` のままフォーカスを受け、`aria-describedby` が
  Modal 自身の持つ理由へ結ぶ（見た目には出さない。同じ理由を下部操作行が既に刷っているため）。
- **`ShortcutHelp.svelte`・`ProjectRegister.svelte` から文言ボタンを外し、`onclose` prop も外した**
  （どちらも出口を持たなくなったため）。設定モーダルの「変更せずに閉じる」は AC #4 の対象外。

## 画面横断契約

`Modal.component.test.ts` の「この層が持つ出口は Escape だけで、他に出口を足さない」を
**「× と Escape の 2 つで、他に出口を足さない」**へ書き換えた。1 件足した — 「閉じられない理由を
渡されている間、× は要求を出さず理由へ結ぶ」。`App.component.test.ts` は 2 経路 → 3 経路
（Escape・×・変更せずに閉じる）で、保存の発行中はその 3 つのどれも閉じないことも同じ形で固定した。

## 実測（借り物 playwright。WebKit / Chromium、1280×800、`_sandbox/settings-check/` に
`?modal=` のつまみを足した）

- × の位置と大きさは **3 モーダル・2 エンジンとも同一**: ダイアログの上端・右端から 9px
  （`--modal-close-inset` 0.5rem ＋ 枠 1px）、控え 24×24px、図形 14.39×14.39px（0.9rem の 1em）。
  AC #1 の「同じ位置・同じ見た目」はこの一致で満たしている。
- 見出しの**文字**と × の間隔は 512〜632px で、どのモーダルでも重ならない。縦の中心のずれは
  0.59〜2.5px（原文どおり見出しと同じ行に見える）。**h2 のブロック箱は × の下まで伸びる**が、
  これは全幅ブロックの箱であって文字ではない（最初の測り方がこれを重なりと読んだので測り直した）。
- 保存の発行中は **両エンジンとも 破線枠・opacity 0.45・cursor: help** の 3 つが揃う
  （doc-11 §2.3 の無効化提示）。`border: 1px solid transparent` のままだと `app.scss` の
  `border-style: dashed` が色の無い枠を描くので、`[aria-disabled="true"]` で色を戻している。
- フォーカスは 3 モーダルとも開いた直後に × へ入る。

**目視は完了し、指摘は無かった（2026-08-05）。** 実機 WKWebView での 3 モーダルの見え方・
フォーカスリングの見切れ（TASK-74 で実機だけに出た型）・図形の太さのいずれも問題なし。

**目視は「Tab で × に移してもアウトラインが強調されない」で始まったが、実測の結果 欠陥ではなかった。**
**WebKit の既定の Tab 順は `<button>` を飛ばす**（macOS の Full Keyboard Access が入っていない状態）
ので、フォーカスがダイアログの外にある 1 打鍵だけ × がスキップされ、次のテキスト入力へ着いていた。
× にフォーカスが来ていなかっただけで、来れば `:focus-visible` は立つ（両エンジンで `outline: solid 2px`
を実測。巡回が呼ぶ `element.focus()` 経由でも立つ）。**リングが出ない＝そこに
フォーカスが無い、と読んでよい。**

同じ実測で**フォーカスの罠が破れていないことも確かめた**。モーダルの背景を押すと
`document.activeElement` は `body` になるが、**次の Tab は背後の画面ではなくダイアログの中へ戻る** —
クリックが sequential focus navigation の起点を背景（＝モーダルの DOM 内）に置くためで、
doc-7 §2.1 の「フォーカスを内側に留める」は成立している。`activeElement === body` を「罠が外れた」と
読まないこと。

**残る未測定は 10 テーマでの × のコントラストだけ**（実測したのは `atlas-light`）。非文字要素なので
WCAG の下限は 3:1 で、色は `currentColor`＝`--fg` を継ぐため `--fg` 対 `--panel` の比に等しく、
decision-12 の収録条件がそれより厳しい比を全テーマで通している。**測っていないことに変わりはない**
ので、族の色を使う TASK-77 が閾値の測り方を決めるときに併せて当たるとよい。

## 検証

`pnpm test` 575（+1）・`pnpm run check` 288 ファイル 0 エラー・`pnpm run build` 成功。
`cargo test` 347 passed / 4 ignored（Rust は触っていない）。フロントエンドに設定された
フォーマッタは無い（prettier は依存にも設定にも無い）ので、静的解析は `pnpm run check` が担う。
<!-- SECTION:NOTES:END -->
<!-- SECTION:NOTES:END -->
