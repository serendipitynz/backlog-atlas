---
id: TASK-164
title: 画面全体の文字サイズと本文の可読性を見直す
status: In Review
assignee: []
created_date: '2026-08-13 08:34'
updated_date: '2026-08-14 07:21'
labels:
  - ui
  - readability
  - 'kind:improvement'
milestone: m-3
dependencies: []
priority: medium
ordinal: 157700
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
TASK-118 の実機目視 (2026-08-13) 由来。ユーザーが backlog browser と Atlas を並べて比較し、**「マークダウンのレンダリングは正直 backlog browser の方が読みやすい。Atlas は全体的にテキストサイズが小さい」** と述べた。**ユーザーが「別タスクで調整したい」と明示している。**

対象は 1 画面ではない。本文の整形表示 (doc-8 §9・doc-11 §14) はタスク詳細・文書・マイルストーン・決定事項の 4 か所が同じ規則を引いており、文字サイズは `app.scss` と各コンポーネントに散っている。**どこが基準の 1 か所なのかを先に数える** — doc-11 §2.2 の寸法段階が文字サイズを持っているか、持っていないなら「実装が何か所で別々に決めているか」を数えるところから始める (TASK-141 がフォーム部品の高さで踏んだ型)。

**比較対象の実物がある**: backlog browser を同じ台帳で開けばよく、ユーザーが画面写真を出せる。
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 文字サイズを決めている箇所を実装から数え上げ、基準の置き場を 1 か所に決めて doc-11 へ書いた
- [x] #2 整形表示の本文について、変更前後の寸法を実エンジンで測って記録した (行の高さ・段落間・見出しとの比)
- [x] #3 4 か所 (タスク詳細・文書・マイルストーン・決定事項) すべてに同じ基準が効いていることを検査で保つ
- [ ] #4 ユーザーの目視で backlog browser と並べて読みやすさを確認した
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## 数え上げ（受入条件 #1）

**2 つの軸で数えた。** 片方だけでは、継承で決まっている箱（ソースの字面が漏らす）と、1 つの宣言が複数の箱に効いている重複（描画結果が膨らませる）を区別できない。

| 軸 | 変更前 | 変更後 |
|---|---|---|
| ソースの `font-size:` 宣言 | **186 件・18 ファイル・21 通りの値**（rem 179・`em` 4・別名 3）。`app.scss` に 0 件、共有の変数を引くものも 0 件 | **187 件・19 ファイル**（画面の 186 件は動かず、増えた 1 件は `app.scss` の 地の 110% である）。**画面が書いた rem 実数は 0 件**（`em` 4 件は箱に対する比なので段ではない。別名 `--frame-text` も段を指す） |
| 実エンジンが描いた段 | スイムレーン 10 通り／タスク詳細 15 通り／プロジェクト詳細 11 通り | **6 通り**／8 通り／8 通り（増分はどちらも `0.95em` のインラインコードと mermaid 自身の字で、段を選んでいない） |

`--frame-text` は `TaskDetail.svelte` が自分の中で `0.7rem` と定義していた別名で、共有ではなかった。

**契約は前からあり、実装が読んでいなかった。** doc-11 §2.2 は当時も字の大きさを 7 行持っていたが、`App.svelte` の `h1` は本表が `.92rem / 650` と書いている隣で `1rem` を書き、太さは UA の 700 のままだった。**「実装が既にやっている」は「契約がある」ではない**の実例なので doc-11 §2.2 に記録した。

## 決めたこと

**置き場は `app.scss` の `:root` である**（doc-11 §9 の 2 つ目に追記）。これは doc-11 §9 の既存の但し書き「複数コンポーネントが参照するものだけを変数として置く」の適用結果ではなく、**その但し書きの外に出した** — 1 コンポーネントでしか使わない大きさであっても段を取る。「ここでしか使わないから」を許した結果が 186 件だったためで、decision-12 が色値に与えているのと同じ形にした。

**文字寸法段階は 6 段・幅一定 0.06rem**（`.62 / .68 / .74 / .80 / .86 / .92rem`）。既存 18 値の割り当ては **最寄りの段へ寄せる**規則で機械的に決め、**同点は大きい側へ寄せた**（このタスクは字が小さいことを直すものなので、同点で小さい側を選ぶと目的と逆を向く）。**動いた幅の最大は 0.08rem** で、`1rem` だった 2 件（`h1` と ☰）だけがそれに当たる。残りは 0.04rem 以下。

**幅を一定にしたのは、狭い段が段として働かないためである。** 置き換えた 18 値には `.74/.75`（0.01 差）と `.92/.95`（0.03 差）があり、隣り合う 2 つを別の大きさとして読める画面は無かった。検査は「ある幅以上」ではなく**一定であること**を見る — 下限だけでは一部だけ詰まった梯子が通る。

**画面設計案 06 のタイポ 2 行から意図的に外れた**（doc-12 §7.1、逸脱表に行を足した）。原文の 6 値を含む一定幅の梯子が無いためで、外れたのは タスク title（`.95` → `.92`）と カード title（`.78` → `.86`）だけ。残る 4 行は原文の値か原文の範囲の中にある。

## 大きくした手段（ユーザーの選択、2026-08-14）

**地の 1rem を 110% へ上げた。この 1 行が readability の変更の全部で、段の rem 値は §2.2 が前から持っていた数のままである。** `px` ではなく `%` にしたのは、利用者が OS 側で字を大きくしている場合に `px` がそれを上書きするため。

**段だけを上げる案は実測して退けた**（WebKit、2026-08-14）。余白が付いてこないので ×1.18 でカードの 印 と priority チップが別行へ落ちた。×1.10 は持ちこたえたが、行長上限 48rem・余白・フォーム部品の高さが据え置きのままになる。画像は `_sandbox/text-scale-check/shots/`（`0-before` 〜 `4-text-118`）。

## 実測（受入条件 #2）

**ハーネスの設定は、doc-11 §2.2 が記録済みの 21.59px / 22.39px を再現できることで先に確かめた**（変更前、両エンジンとも一致）。手順は `_sandbox/text-scale-check/measure.mjs`。

整形表示（WebKit・Chromium とも同値。**タスク詳細・文書・マイルストーン・決定事項 の 4 か所すべてで同じ数**）:

| | 変更前 | 変更後 |
|---|---|---|
| 本文 | 11.84px | **13.02px** |
| 行の高さ | 17.76px | 19.54px |
| 段落の上下余白 | 4.80px | 5.27px |
| h1・h2 | 12.48px（本文の **1.054 倍**） | 15.14px（本文の **1.162 倍**） |
| h3 以下 | 11.84px（本文と**同値**） | 14.08px（本文の 1.081 倍） |

**見出しの段を広げたのはユーザーの選択である**（同日）。2026-08-11 の目視では許容されていた。**h3 以下が本文と同じ大きさだったことが、段の別を読み取れなかった主因**で、あの状態の h3 は太さと余白しか本文と違わなかった。

**doc-11 §13 の帯も測り直した**（§13 に表を足した）。地を上げると固定行だけが伸びて窓は伸びないので帯は縮み、規則の右辺 1.75rem は 28px → 30.8px へ動く。**6 面のうち 3 面を測れた**（タスク詳細の閲覧・編集、設定モーダル。640×480 で上限の 7.2〜10.8 倍）。**残る 3 面は測れていない** — スイムレーンは測り方が同時に貼り付いていない固定行まで足しており、他の 2 面はこの標本ではスクロールする面が立たなかった。**規則を割った面は 1 つも無い。** **3 面の測り直しは TASK-173（m-4）が持つ。**

## 検査（受入条件 #3）

`src/lib/text-scale.test.ts`。**4 か所の一致を横並びに測る形にしていない** — 一致は 4 つが同じ 1 つの `Body.svelte` を描いていることの帰結であって、帰結を測っても 5 か所目が自前の本文を描いたときに何も起きない。押さえるのは**本文を描く経路が 1 本しか無いこと**である。

**8 通りの壊し方を実際に当てて、8 件とも落ちること、かつ落ちた指摘が意図したものであることを確かめた**（画面に長さを直書き／段の幅を不揃いに／段を降順に／地の外に 2 つ目の `font-size`／別名を数へ戻す／整形表示の見出しを本文と同じ段へ／`<Body>` を自前の描画へ置換／別コンポーネントに `.body-block` を書く）。**走査対象が空でないことも別の case が見る** — `app.scss` は `?raw` では中身が 0 バイトで返り、キーは在るので `typeof` を見る検査は素通りする（実測）。`theme.test.ts` と同じく sass に開かせて解決した。

## 走らせたもの

- `pnpm test` — 879 件。**1 回目に `markdown-figure.component.test.ts` の 1 件が 5,000ms で timeout**（ファイル自体は 6,608ms）。**TASK-150 の兆候の 9 例目で、8 例目までと同じくフルスイートの 1 回目**。落ちた case 名は `keeps the fence inside the 本文 and adds nothing outside it`。2 回目は 879 件すべて通った。
- `pnpm run check` — 0 errors / 0 warnings（`_sandbox/app-check/vite.config.ts` の 1 行は既知）。
- `pnpm run build` — 成功。
- **Rust 側は触っていない**ので `cargo test` は走らせていない。

## 残っているもの

**受入条件 #4（backlog browser と並べた目視）はユーザーの操作である。** この環境は `screencapture` が使えないので、数と画像で取れるところまでを上に置いた。画面は `_sandbox/text-scale-check/shots/final-*.png`（スイムレーン・設定モーダル・絞り込み・プロジェクト詳細）と `after-webkit-*.png`（タスク詳細・決定事項）。

## PR #118 のレビューを受けた改め（1R、Codex、2026-08-14）

**4 件すべてを受け入れた。取り下げた指摘は無い。**

- **[P2] 検査が `var(--…)` を名前で通していた。** 画面が `--caption-size: 0.5rem; font-size: var(--caption-size)` と書けば通っていた — 別名を見る case が名前に `text` を含むものだけを対象にしていたためである。**`var()` を定義まで辿って段に届くかで判定する形へ改めた**（`resolvesToAStep`）。名前で選ぶ形が穴そのものだったので、**名前で選ぶ専用の case は落とした** — 残すとそちらが番をしているように読める。**別名が 2 か所で定義され片方だけが段を指す形も通さない**（すべての定義が段に届くことを要求する）。循環は段に届かないものとして扱い、検査が止まらないようにした。
- **[P2] 段の値が doc-11 §2.2 と突き合わされていなかった。** 昇順・幅一定・6 段だけを見ていたので、**梯子ごと平行移動しても通り**、そのとき doc-11 の表と、その表を基準に測った §13・§14.1 の実測がまとめて偽になる。 **doc-11 §2.2 の 文字寸法段階 の行から 6 値を読んで比べる形にした。** **期待値をテストに書かない** — 書けば doc と `app.scss` に並ぶ 3 つ目の写しになり、このタスクが取り除いた形に戻る。**表の行が消えた場合も、比べる相手が無いことを黙って通さない**（行が見つからないことを名指しして落ちる）。
- **[P2] doc-11 §2.2 で閉じる `**` の後の半角スペースが 1 か所欠けていた**（`。**利用者` → `。** 利用者`）。**指摘より広い範囲が壊れていた** — 描画すると `<strong>` が入れ子になり、強調の範囲が約 200 字ぶんずれて、意図した太字（「全体的にテキストサイズが小さい」に対する変更はこの 1 行だけである）と、太字でないはずの文が入れ替わっていた。**自分の検査が取り逃した理由は 2 つある**: (1) literal の `**` を数える形は「対がずれた `**`」を捕まえない（指示書が警告している型）、(2) 入れ子の検出に `/<strong>[^<]*<strong>/` を使っており、**間に `<code>` が入ると当たらなかった**。**深さを数える形に改めて文書全体を旧版と突き合わせ、この 1 行だけがこの PR 由来であることを確かめた**（doc-12・task-164・task-173 は 0 件）。
- **[P3] 変更後の宣言数が 186 件ではなかった。** 正しくは **187 件・19 ファイル**（画面の 186 件は動かず、増えた 1 件は `app.scss` の 地の 110%）。**`grep -c 'font-size:'` は 188 を返すが、うち 1 行は `Modal.svelte` の註**（`font-size:` の後に `;` が無い）で、宣言ではない。

**改めた検査に対して 5 通りの破壊を当て直し、5 件とも「それを名指しする case で」落ちることを確かめた**（名前が `text` を含まない別名の経由／別名が数へ戻る／梯子ごとの平行移動／doc の側だけを動かす／表の行の改称）。**旧 8 件も通し直し、8 件とも落ちたままである。**

`pnpm test` 880 件通過（この回は 1 回目から通り、TASK-150 の兆候は出ていない）。`pnpm run check` 0 errors / 0 warnings。

## PR #118 のレビューを受けた改め（2R、同日）

**1R の 4 件はすべて解消と判定された。新規は [P3] 2 件で、両方受け入れた。**

- **[P3] `var()` を辿る関数が `seen` を枝の間で共有していた。** `--a` を `var(--mid)` として 2 回定義し `--mid` が段に届く形で、共有版は `false` を返す（再現済み）。**これは正しい木を落とす向きの誤りで**、その形を最初に持ち込んだ画面で「説明のつかない失敗」として出るところだった — 違反の見逃しではない。 **`seen` を「そのときの経路」にし、枝ごとに複製する形へ改めた。** 指摘どおり case を 2 つ足し（同じ中継を 2 度通る形・2 本の枝が共通の中継を通る形）、どちらも共有版へ戻すと落ちることを確かめた。
- **[P3] ブロック省略が 5 か所あった**（99・101・103・163・164 行）。リポジトリの明示ブロック規則どおり波括弧を付けた。

**3R で全件解消と判定され、bot (`simpleboxes`) 名義で APPROVED が `4e122fc` に対して記録された。P1 は 3 ラウンドで 1 件も出ていない。**
<!-- SECTION:NOTES:END -->
