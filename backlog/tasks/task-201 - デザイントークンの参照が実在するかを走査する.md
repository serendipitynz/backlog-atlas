---
id: TASK-201
title: デザイントークンの参照が実在するかを走査する
status: In Review
assignee: []
created_date: '2026-08-27 23:43'
updated_date: '2026-09-20 08:23'
labels:
  - test
  - ui
  - 'kind:chore'
milestone: m-4
dependencies:
  - TASK-83
priority: low
ordinal: 192700
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
`var(--…)` が実在する **デザイントークン**（doc-11 §2）を指しているかを確かめる走査を置く。**入力は `src/` の `.svelte` と `.scss` のスタイル全部である。**

**起票の由来は TASK-83 の実機目視である**（2026-08-28、オーナー）。サブメニューの CSS が `var(--border)` と `var(--warn)` を書いており、**どちらも `app.scss` に無いトークンだった**。**CSS は無効値のとき宣言ごと捨てる**ので、`border: 1px solid var(--border)` は枠を 1 本も描かず、`color: var(--warn)` は色を変えなかった。**画面は「不自然」に見えるだけで、どの検査も赤くならなかった。**

**この型が静かに通る理由は 2 つある。** ①`pnpm run check` は Svelte の `<style>` の中の CSS を型検査しない。②`pnpm run lint` は Biome で規則 1 つ（`style/useBlockStatements`）だけを有効にしており、CSS を読まない（decision-32）。**したがって現在この木にトークン名を確かめる機構は無い。**

**着手時に数え直すこと。** TASK-83 の回に測った時点では、外していたのは上記 2 つだけだった（`app.scss` の定義 23 個、`var()` での参照 50 個、コンポーネント側でローカルに定義しているもの 37 個）。**「未解決」に見えて正しいものが 13 個ある** — inline の `style="--x: …"` で与えているもの（`--section-nav-width`・`--panel-padding` など）で、**走査はこれを偽陽性にしてはならない。** これが本タスクの主な難所である。

**決めることが 2 つある。**

- **どこに置くか。** 既存の走査（`screen-text.test.ts`・`comment-citation.test.ts`・`sandbox-reference.test.ts`）と同じ `unit` の一員にするのが素直だが、**入力がソースの木そのもの**である点はあの 3 本と同じなので、あちらの読み方（ファイルを読んで正規表現で当たる形）を踏襲できるかを見る。
- **inline で与える名前をどう扱うか。** `style="--x: …"` の宣言を走査自身が集めるか、あるいは「inline で与える名前は `app.scss` の隣に宣言する」といった規約を先に置くか。**前者は走査の側が 2 つの構文を読むことになり、後者は規約を守る機構が要る。** どちらを採るかは着手回の判断で、決めたら doc-11 §2 か decision-32 のどちらに書くかも併せて決める。

**m-4 とした理由**: 走査そのものは利用者が受け取るものを変えない（開発側の検査である）。**TASK-83 が入れた欠陥は既に直っており**、次に出す版に未対応のまま載るものではない。

**依存に TASK-83 を置いてある** — あの回の実測（外していたのは 2 つだけ、偽陽性になりうるのは 13 個）が本タスクの出発点だからである。
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 src/ の .svelte と .scss にある var(--…) の参照が、app.scss のトークンかコンポーネント内の宣言か inline style の宣言のいずれかに解決することを走査が確かめ、解決しない参照で落ちる
- [x] #2 TASK-83 が外していた 2 つ（--border・--warn）を戻すと走査が落ちることを確かめる
- [x] #3 inline style で与えている 13 個が偽陽性にならない。inline の扱いをどちらに決めたかと、その置き場（doc-11 §2 か decision-32）が書かれている
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. 走査を `src/lib/design-token.test.ts` へ置く (`unit` の一員)。読み方は `text-scale.test.ts` を踏襲する — 各コンポーネントの `<style>` を sass に通し、`app.scss` も sass に通す (`?raw` では両方 0 バイトになる、という同ファイルの実測による)。`.scss` の partial は素の文面でも読む (mixin を誰も `@include` しなくなっても参照が走査の外へ出ないようにする)。
2. inline `style="--x: …"` は走査自身が markup から集める (着手回の判断)。規約側を採らない理由は、13 個がコンポーネント 1 インスタンスの実引数であって doc-11 §1 の デザイントークン ではないこと、および規約には守る機構が別途要ることの 2 つ。
3. 解決の範囲は木全体の和集合とする。ファイル単位に絞ると、カスタムプロパティが DOM を通して子コンポーネントへ継承される正しい形 11 件 (`--prose-max-width`・`--list-column-width`・`--panel-padding`・`--modal-close-*`) が偽陽性になる — 実測 2026-09-20。
4. 補間 `var(--#{$x})` は静的に解決できず、参照の正規表現にも当たらない (黙って走査の外へ出る)。今 0 件であることを別の case で押さえ、将来 1 件でも書かれたら赤くする。
5. AC #2 は、走査が使う解決器へ実際の定義集合と `--border`・`--warn` を与えて解決しないことを押さえる形で持つ。あわせて実物のコンポーネントへ 2 つを戻して赤くなることを 1 度測り、Implementation Notes に書く。
6. 置き場の決定を doc-11 §2 へ書く (decision-32 は「doc-11 のデザインシステムの規則の機械化は別の判断」と自身の射程外を明言しているため、AC の 2 択のうちこちらしか採れない)。§9 の 写し方 の列挙にも走査を名指しする 1 行を足す。
7. `pnpm test`・`pnpm run check`・`pnpm run lint` を走らせる。
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
`src/lib/design-token.test.ts` を置いた（`unit` の一員、8 case）。doc-11 に §2.5 参照が解決すること を新設し、§9 の 写し方 の列挙に 1 行を足した。

## 着手時の数え直し（2026-09-20）

TASK-83 の回の実測と完全に一致した — `app.scss` の定義 23、`var()` 参照 50、木全体の宣言 37、inline 13、未解決 0。

## 決めた 2 つ

**置き場は `unit` の走査 1 本。** `text-scale.test.ts` が既に「各コンポーネントの `<style>` を sass に通し、`app.scss` も sass に通す」機構を持っているので、その読み方をそのまま継いだ。`?raw` では `.scss` が 1 バイトも読めないことを測り直してある（`app.scss` と 2 つの partial、3 ファイルとも `string` の長さ 0。**`typeof` を見る検査は素通りする**）。sass に通す形は補間も解くので、`var(--#{$name})` を素の文面から名前として取り出せない問題はここには生じない。

**inline は走査自身が `style` 属性から集める。** 規約側（「inline の名前も `app.scss` の隣に宣言する」）を採らなかった理由は 2 つで、① 13 個は `--columns`・`--lane-top`・`--title-lines` のようにコンポーネント 1 インスタンスについて実行時に決まる値であり、doc-11 §1 のデザイントークン（複数の画面が同じ値を参照するために名前を与えたもの）ではない — `app.scss` へ並べるとトークンの集合そのものが偽になる。② 規約には守らせる機構が別に要るので、走査が 2 つの構文を読むほうが安い。

**置き場を doc-11 §2 にしたのは、decision-32 が自分の射程外を明言しているからである。** 「Stylelint を採らない」の項が「doc-11 のデザインシステムの規則を機械化するかどうかは別の判断であり、この decision はそこに触れない」と書いており、受入条件 #3 が挙げる 2 択のうち decision-32 は選べない。

## 実測で変わった設計 — 解決の範囲は木全体である

ファイル単位に絞る形を先に試し、**正しい 11 件が偽陽性になることを測った**（2026-09-20）。カスタムプロパティは DOM を通って子コンポーネントへ継承されるので、値を与える側と読む側が別ファイルに居るのが正しい形で、`Body.svelte`・`DetailSection.svelte` の `var(--prose-max-width)`、3 つの一覧区画の `var(--list-column-width)`、`EditIssueRow.svelte`・`Heading.svelte` の `var(--panel-padding)`、`App.svelte`・`CreateLayer.svelte` の `var(--modal-close-*)` がこれにあたる。受入条件 #1 が挙げる 3 択もこの読み方である。**そのぶん、別の画面のために置かれた名前を借りた参照はここでは落ちない** — doc-11 §2.5 にその限界を書いた。

## 受入条件 #2 の測り方 — 2 通りで測った

- **走査の判定器に対して**: 実物の宣言集合へ `var(--border)`・`var(--warn)` を持つ合成 CSS を与え、2 つとも報告されること、同じ関数が実在するトークン（`--fg`・`--line`）を通すことを押さえた。判定器に名前で選ぶ枝が無いので、`--border` が特別なのではなく木に宣言が無いことが特別である。
- **実物のコンポーネントに対して 1 度**: `HeaderMenu.svelte` へ `border: 1px solid var(--border);` と `color: var(--warn);` を書き戻して走らせ、走査が `../components/HeaderMenu.svelte: --border` と `--warn` を名指しして 2 case が赤くなることを見てから戻した。

## 走査が自分の入力を押さえる 4 case

「解決しない参照が 0 件」は入力が痩せても緑になるので、入力側を別に押さえた。**4 つとも変異させて赤くなることを確かめてある。**

- 出荷される CSS の量（`app.scss` 1000 字超・コンポーネント 15 本超・参照 40 種超）。
- **区画が分け合う SCSS に届いていること** — `loadedUrls` に 2 つの `_shared.scss` が在ること、および素の文面より sass を通した側のほうが参照が多いコンポーネントが 1 つ以上在ること。sass を通さない版に変異させるとこの case だけが赤くなる（参照が消えるだけなので、未解決の数では気づけない）。
- **`style` 属性を 1 つも取りこぼしていないこと** — `style=` の出現数と切り出せた数が全ファイルで一致すること。
- **名前そのものを実行時に組み立てている宣言・参照が 0 件であること** — `style="--{name}: 1"` も `var(--{name})` も静的に解決できず正規表現にも当たらないので、黙って走査の外へ出る。今 0 件であることを押さえて、1 件でも書かれたら赤くする。

**この過程で自分の実装の穴を 1 つ見つけた。** 属性を引用符ごと切り出した初版は、宣言の手前に区切り（`{`・`;`・空白）を求める正規表現と噛み合わず、**属性の先頭に書かれた宣言を 5 個落としていた**。残る 8 個が通っていたので「未解決 0 件」は緑のままで、赤くしたのは「外して出てきた名前はすべて inline が与えているもの」を見る case だった。

## 走査の外に置いたもの

**誰も `@include` しない mixin の中の参照は対象外である** — 出荷されないためで、穴ではない。現時点で未使用の mixin は 0 件（`task-detail/_shared.scss` 27 個・`project-detail/_shared.scss` 54 個をすべて確認）。

**宣言が在ることだけを見て、値が妥当かどうかは見ない。**

## 検査

`pnpm test` 53 files / 1370 tests 通過、`pnpm run check` 0 errors 0 warnings、`pnpm run lint` clean。GUI E2E・規模計測・実 OS 通知の配送検査はこのセッションの検査ではない（AGENTS Tests）。

## 範囲外として上げておくもの

`comment-citation.test.ts` は**自分自身のファイル名をコード註に書けない**。`import.meta.glob` が呼び出し元のモジュールを結果から外すので、`PATHS` にそのファイルだけが入らず、註が `comment-citation.test.ts` を名指しすると「tree にも一覧にも無い」と報告される（実測 2026-09-20）。本タスクの走査の頭註が最初にこれを踏んだので、引用先を `third-party-licenses.test.ts` へ替えて回避してある。本タスクの範囲外なので直していない。
<!-- SECTION:NOTES:END -->
