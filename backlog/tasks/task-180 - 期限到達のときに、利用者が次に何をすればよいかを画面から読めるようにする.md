---
id: TASK-180
title: 期限到達のときに、利用者が次に何をすればよいかを画面から読めるようにする
status: To Do
assignee: []
created_date: '2026-08-15 03:56'
updated_date: '2026-08-18 21:11'
labels:
  - 'kind:enhancement'
milestone: m-4
dependencies: []
priority: low
ordinal: 171700
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
TASK-179 の実測から出た別件である。**期限到達の画面文は現在「30 秒以内に終了しなかったため中断しました」だけ**（`edit.ts` の `failureKindDetail`）で、原因も、利用者が次に何をすればよいかも述べない。

**実測（2026-08-15、TASK-179 に全表）では、この 36 秒のうち 99.3% が git のコストで、`config.yml` の 2 つのキーだけで 143 倍変わる** — `check_active_branches: false` で 36.0s → 3.47s、さらに `filesystem_only: true` で 0.252s。**つまり利用者が対処できる事象なのに、画面はそれを示していない。** オーナーは実機でこれを踏み、原因の特定に調査を要した。

**ここには容易でない点が 1 つある。** **Atlas の `Config`（`domain.rs`）が読むのは `project_name`・`task_prefix`・`statuses`・`default_status`・`date_format` の 5 つで、`check_active_branches` も `filesystem_only` も読んでいない**（2026-08-15 実測）。したがって「その設定が原因らしい」と名指しするには**読み取り層を広げる必要があり、それは doc-4 の範囲を変える。** 費用に見合うかどうかを含めて判断する。

**安い側の案**: 設定を読まずに、期限到達の理由文へ「この操作は Backlog CLI が git を走査する設定だと極端に遅くなることがある」旨と確認先を書くだけでも、調査の入口にはなる。**高い側の案**: `Config` を広げ、当該設定が有効なプロジェクトでだけ具体的に名指しする。

**doc-11 §5 の「理由の無い無効化を置かない」の同族だが、あちらは無効化の話で、こちらは失敗の理由文である。** 置き場は doc-5 §5（CLI 失敗時の扱い）と doc-11 §8（画面に置く文）。

**期限そのものの妥当性は TASK-179 が持つ。本タスクは、期限に達したときに何が読めるかだけを扱う** — 期限を上げても、いずれ超える利用者には同じ文が出る。

## m-4 へ移した（2026-08-15、同日）

起票時は m-3 とした。根拠は「Windows 利用者が期限到達を踏み、原因が分からない」であったが、**切り分けの結果その入口は wslgit 固有で、通常の Windows は 0.72 秒・余裕 41 倍だった**（TASK-179 の表）。**期限到達を踏む利用者が居ないなら、次リリース基準の「行動を誤らせる」に当たらない。**

**それでも残すのは、文が現に理由を述べていないからである** — 踏んだときに「30 秒以内に終了しなかった」だけでは、利用者は自分の側に手当てできる原因があること自体を知らない。**踏む頻度が下がったので急がないが、直す価値は消えていない。**
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 期限到達の画面文が、利用者が次に確かめられることを示している
- [ ] #2 読み取り層を広げるかどうかが理由付きで決まり、広げるなら doc-4 の改訂を伴っている
- [ ] #3 理由文の置き場が doc-5 §5 と doc-11 の該当節に書かれている
<!-- AC:END -->
