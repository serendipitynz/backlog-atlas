---
id: TASK-193
title: AGENTS の Release 節を配布・リリース層の doc へ移し、AGENTS はポインタだけ持つ
status: Done
assignee: []
created_date: '2026-08-18 20:39'
updated_date: '2026-08-19 01:01'
labels:
  - docs
  - release
  - 'kind:chore'
milestone: m-3
dependencies: []
priority: medium
ordinal: 184700
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
`AGENTS.md` の Release 節一式が、リリースする回にしか読まれないのに毎セッション読まれている。
配布・リリース層の正本を doc へ移し、AGENTS にはポインタだけ残す。

**実測（2026-08-19、`AGENTS.md` 41,548 文字）**

- **毎セッション読まれるのは `AGENTS.md` だけである。** `CLAUDE.md` はそれへの symlink で、
  `AGENTS.ja.md` は開いたときにしか読まれない。日本語版は 1 文字あたりのトークン単価が高い
  （多バイト 52%、概算 16,300 トークン）が、常時課金されているのは英語版の**概算 10,600 トークン**である。
- **Release 節一式は 15,064 文字で、`AGENTS.md` の 36% を占める。** 内訳は Release 918・
  Producing a release 3,623・**Third-party notices 5,950**・Bundle metadata 2,313・
  macOS signing and notarization 2,260。
- **m-3 の 41 件のうちリリースは TASK-181 の 1 件である。** つまりこの約 3,800 トークンは
  40 回払って 1 回使っている。

**移す理由は節の長さではなく、正本が無いことである。** `backlog/docs/` は doc-1〜12 まで
**全部が画面と読み書き層の設計**で、配布・リリース層の正本が 1 つも無い。そのため AGENTS が
正本を兼ねており、36% はその帰結である。同じ層の知識は `.github/workflows/release.yml`・
`scripts/generate-third-party-licenses.mjs`・`scripts/macos-*.sh` にも散っている。

**決定先行として扱う。** doc の新設は契約の変更である。加えて Release 節の中には
「この workflow について 6 つは詳細ではなく決定である」と自ら述べている塊があり、
**doc へ移すものと decision として起こすものの切り分けが着手時の判断になる。**

**着手時に確かめること**

- **ポインタに要約を置かない。** 要約があると読まずに素通りされ、移した意味が消える。
  「リリースの回は最初に doc-N を読む」という形にする。
- **`AGENTS.md` と `AGENTS.ja.md` は同内容でなければならない**（AGENTS 自身の規則）ので、
  両方を同時に動かす。
- **doc の本文は `backlog doc update <id> --content` で書く**（往復はバイト同一）。
  decision を起こすなら本文は直接編集（decision-27 §7 を書いた TASK-162 と同じ）。
- **移した後に `AGENTS.md` の文字数を測り直し、削減額を Implementation Notes に書く。**
  見込みは約 3,500 トークン（33%）だが、ポインタの長さで変わるので実測する。

**削らないと判断したもの（TASK-162 の回に測った）**

- **Coding style の 9 bullet のうち 5 つがグローバル `~/.claude/CLAUDE.md` と逐語で重なる**
  （約 900 文字 ≒ 250 トークン）。両方常時読まれるので純粋な二重払いだが、
  **decision-32 が「コーディング規約を AGENTS に明示する」と決めている**ので、
  消すには decision-32 の改訂が要る。額が釣り合わない。
- **Toolchain 後半の 7 bullet（約 4,400 文字）** は、読んだ人が欠陥だと誤認して壊すのを
  止めるために置かれている（`dragDropEnabled` の削除、航行ゲートを未使用と判断、
  `KNOWN_SCHEMA_VERSION` の上げ忘れ、About パネルのアイコン）。正本は decision-13/34/37/38 に
  あるが、**decision を開く動機は「そこに何かある」と知った後にしか生まれない**ので、
  AGENTS 側の 1 行を消すと参照経路ごと消える。
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 配布・リリース層の正本となる doc が新設され、AGENTS の Release 節が持っていた内容がそこにある
- [x] #2 AGENTS.md と AGENTS.ja.md の Release 節が、要約を持たないポインタになっている（両言語で同内容）
- [x] #3 doc へ移すものと decision として起こすものの切り分けが済み、decision を起こしたならその理由が本文にある
- [x] #4 移した後の AGENTS.md の文字数と概算トークンを測り直し、削減額が Implementation Notes に記録されている
- [x] #5 リリース手順の記述が、workflow・scripts の実物と食い違っていないことを確かめてある
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## 成果物

- **doc-13「タグから利用者が受け取る資産までの配布層 設計」を新設した。** 配布層の正本であり、
  Release 節一式が持っていた内容はここにある。
- **decision-39「第三者依存のライセンス通知は自前の生成器で作り生成物をコミットする」を起こした。**
- **AGENTS.md と AGENTS.ja.md の Release 節を、doc-13 への素のポインタと 4 行に置き換えた。**
  下位の 4 見出し（Producing a release・Third-party notices・Bundle metadata・macOS signing and
  notarization）はどちらの言語からも無くなった。**4 行が残る条件は 1 つで、リリース以外を契機に破られ、
  かつ破られる場所にその旨を書けない規則であることだ。** doc-13 の冒頭が同じ 4 つを名指ししている。

## doc と decision の切り分け（AC #3）

**採否だけを decision へ切り出し、残りは doc へ移した。** 切り分けの規則は「候補が 2 つ以上あって
一方を退けた判断か」である。**この repo に既にあった decision 38 件はすべてこの形をしており、doc-1〜12 に
この形は 1 つも無い。** 配布層の採否は既に decision-7・26・30 が持っているので、同じ層の採否が
doc と decision に分かれない形を選んだ。**ユーザーが 2026-08-19 に選定した。**

decision-39 に入れたのは 5 つで、どれも退けた候補を持つ — `cargo-about` を採らない・CI 生成でなく
コミットする・無フィルタのグラフでなく 5 トリプルの和集合を採る・展開済みソースでなく `.crate` から
読む・本文の無い識別子で止まる。**数と手順は入れず doc-13 §4 に置いた。**

## 実測（AC #4）

| ファイル | 前 | 後 | 差 |
|---|---|---|---|
| `AGENTS.md` | 43,086 文字 | 29,203 文字 | **-13,883（-32.2%）** |
| `AGENTS.ja.md` | 26,279 文字 | 17,303 文字 | -8,976（-34.2%） |

**この表は 1R の指摘 2 を直した後の値である。** 残す行が 2 行から 4 行に増えて `AGENTS.md` が
475 文字戻り、削減は -33.3% から -32.2% になった。

**毎セッション課金されるのは `AGENTS.md` だけなので、削減額はその 1 行が答えである。** 概算トークンは
**約 10,990 から約 7,450 へ、-3,540（-32.2%）である。** **この概算は本タスク本文の実測（41,548 文字
≒ 10,600 トークン）から出した 1 トークン 3.920 文字を当てたもので、トークナイザで数え直した値ではない。**
見込みの「約 3,500 トークン（33%）」と一致した。

**起票時の実測 41,548 文字は 43,086 文字に増えていた** — TASK-161 が「作業上の規約」へ追記したぶんで、
節の内訳（Release 一式が 15,064 → 15,173 文字）はほぼ動いていない。

**足したぶんは doc-13 が 11,360 文字・decision-39 が 2,955 文字で、どちらもリリースの回にしか読まれない。**

## 実物との突き合わせで見つけた 3 件（AC #5）

`release.yml`・`generate-third-party-licenses.mjs`・`macos-*.sh`・`tauri.conf.json`・`Cargo.toml`・
`.gitattributes`・生成済みの `THIRD-PARTY-LICENSES.txt` を読んで突き合わせた。**3 件が実物と違っていた。**

- **「自前のライセンス本文を持たない crate は 19 件」は 25 件だった。** 通知の初版（2026-08-14）から
  25 件なので、**古びたのではなく書かれた時点から偽である。** 族の一覧も `block2` と `dispatch2` を
  落としていた。doc-13 §4.3 に 25 件と内訳を書いた。
- **「和集合 352・無フィルタ 442」は 353・443 だった。** decision-38 が `tauri-plugin-window-state` を
  足したぶんが両方に 1 ずつ乗っている（`32d3bbd`）。doc-13 §4.5 に測り直した値と測った日を書いた。
- **「この workflow について 6 つは詳細ではなく決定である」は網羅ではなかった。** 理由を述べた註は
  実測で 20 を超え、6 つはそのうちの 6 つである（`universal-apple-darwin` で 1 つの `.dmg` に収めること、
  `ubuntu-24.04` を `ubuntu-latest` にしない理由、非 macOS の runner で `APPLE_*` を空に潰すことなどが
  漏れていた）。**doc-13 §3.3 は数え直して 20 を書くのではなく、「なぜそこにあるか」の正本を workflow
  自身の註に置き、doc は写さないと定めた** — 写しは黙って古びるうえ、註を足すたびに数え上げが偽になる。
  **workflow の外まで帰結が届く 3 つ（`.app.tar.gz` の不在・Linux 2 アーキテクチャ・`pnpm test` を
  走らせないこと）だけは doc-13 §3.5 が持つ。**

## AGENTS に残した 2 行と、その理由（AC #2）

**ポインタは要約を持たない。** doc-13 が何を持つかを名前で挙げるだけで、読者が開かずに行動できる主張は
1 つも無い。

**そのうえで 4 行を残した。** 残す条件は 1 つで、**リリース以外を契機に破られ、かつ破られる場所にその旨を
書けない規則であることだ。** 契機がリリース側にある規則は doc-13 にだけ置く — ASCII 制約が AGENTS に
無いのは、`src-tauri/Cargo.toml` がそれを縛る行の直上で述べているからである。本タスク本文が Toolchain の
7 bullet を残した理由と同型である。

| 残した行 | 契機 | その場所に書けない理由 |
|---|---|---|
| README は実装状況の節を持たない（doc-13 §2） | README を編集する回 | **無い節は何も述べない** |
| `bundle.copyright` と LICENSE は対（doc-13 §5） | どちらかを編集する回 | **JSON にコメントを書けない** |
| dev 実行の About アイコンは欠陥ではない（doc-13 §5） | 画面を触る回 | 誤認止めなので置き場が無い |
| 署名名義は意図的（doc-13 §6） | LICENSE と署名を見比べた回 | 同上 |

**初版は下の 2 つだけを残しており、上の 2 つは 1R の指摘で足した** — どちらも契機がリリースではないのに、
doc-13 にしか無い状態になっていた。

## 検査

`pnpm test`（42 ファイル・1,030 件）・`pnpm run check`（529 ファイル・0 エラー）・`pnpm run lint`
（112 ファイル）がすべて通った。**Rust には触れていないので `cargo` は走らせていない。**

**新しく書いた散文を強調記法の字義で数え直した。** doc-13・decision-39・対応表は 0 件で、AGENTS 和英
への追記も 0 件だった（改訂前後の差集合。AGENTS 全体では 29 から 16・32 から 22 へ、移した節ごと
減っている）。**数えるときは行ごとではなく文書全体で `**` の偶奇を取る** — 行またぎの強調があると
行ごとの数え方は閉じ側を開き側と取り違えて黙って見落とす（この回に実際に 4 件見落とした）。

## レビュー 1R への対応

**[P2] 3 件・[P3] 2 件。押し返しは 0 件で、全件直した。**

- **[P2] doc-13 §5 の em dash の実測が escape を落としていた** — `\u2014` と書くべきところが em dash
  その文字になっており、「6 文字」と自己矛盾していた。**escape を戻し、6 文字が何かを字で添えた。**
- **[P2] リリース以外を契機に破られる規則が 2 つ AGENTS から消えていた** — README の実装状況節の禁止と、
  `copyright` と LICENSE の対。**AGENTS に 2 行足し、残す条件を AGENTS と doc-13 の両方に書いた。**
- **[P2] 移した節を指す参照が 3 か所残っていた** — `.env.signing.example` の Prerequisites 行、
  TASK-181 の本文と AC #1。**3 つとも doc-13 の節番号へ向け直した。**
- **[P3] decision-32 の Consequences が同じ引用を持っていた。** 移った旨を添えた。
- **[P3] 日本語のポインタにだけ 1 文があり、英語に無かった（AC #2 の「両言語で同内容」に反する）。**
  **条件文として書き直し、両言語を同じ内容に揃えた。**

**指摘の到達範囲を数え直した。** AGENTS の「リリース」節を指す記述をツリー全体から拾うと、直した 3 か所の
ほかに 4 件あり、**4 件とも Done のタスク本文だった（TASK-90・102・118・170）。** **済んだセッションの記録
なので書き換えない** — `_sandbox/` のパスを Done タスクで直さないのと同じ理由である。**開いているタスクは
TASK-181 だけで、それは直した。**
<!-- SECTION:NOTES:END -->
