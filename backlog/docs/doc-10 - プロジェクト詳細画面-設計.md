---
id: doc-10
title: プロジェクト詳細画面 設計
type: specification
created_date: '2026-07-28 23:04'
updated_date: '2026-08-07 09:26'
---
# プロジェクト詳細画面 設計

画面設計案の反映として書いた設計（実装は TASK-55）。用語は [doc-1](doc-1)・[doc-2](doc-2) に従い、本書で導入する語は初出に定義を置く。
前提は doc-3（台帳・台帳エントリ・status 別名表）・doc-5（Backlog 更新アダプターの操作写像）・doc-9（更新前競合検出・照合不能）・decision-13（アプリ設定は台帳と別ファイル）。画面設計案（doc-1 追補で定義。07）を反映する。引用する事実は doc-12 §8 に転記してある。

## 1. 用語

- **プロジェクト詳細画面** … 1 プロジェクトについて、台帳エントリの編集・登録解除・文書の作成と更新・マイルストーン作成・詳細な新規タスク作成を扱う画面を指す。
- **区画切替** … プロジェクト詳細画面の左側に置く 4 項目（概要・文書・マイルストーン・新規タスク）の選択。画面遷移ではなく、同一画面内の表示切替である。
- **区画ナビ** … 区画切替を収める画面左端の幅 12rem の列を指す（画面設計案 07、doc-12 §8）。区画切替が選択という操作であるのに対し、区画ナビはそれを置く場所で、4 区画すべてが同じ 1 本を共有する。幅の値は content box を名指す（TASK-115 の規約。padding・枠は外側に付く）。
- **一覧列** … 区画の中で、選ぶ対象のカードを縦に並べ、選択を保持する幅 16rem の列を指す。文書区画では文書一覧（§5）、マイルストーン区画ではマイルストーン一覧（§6）がこれにあたる。区画ナビと同じく幅の値は content box を名指す。2 区画が同じ形の列を持つのは偶然ではない — どちらも「対象を選び、選んだ対象に対して操作する」区画で、右の列がその選択で差し替わる（§5・§6）。
- **一覧見出し行** … 一覧列の先頭に置き、並んでいるカードの件数の見出しと、その一覧へ 1 件足す**作成の入口**とを 1 行に並べる領域を指す（TASK-117）。一覧のスクローラの外にあり、一覧をどこまでスクロールしても読める。件数は §5・§6 のとおり**並んでいるカードの数**であって、写せなかったファイルを含めない。
- **作成の入口** … 一覧見出し行に置き、押下でその一覧に対応する**作成モーダル**を上げる操作を指す。それ自身は何も発行しない。**契約の語はこちらで、画面に出る文言は区画ごとに違う**（文書は「新規文書」、マイルストーンは「新規マイルストーン」）— 文言を契約の語にすると、同じ形の操作を 2 区画にわたって指せない。
- **作成モーダル** … 作成フォームだけを載せた被せ層（doc-7 §2.1）を指し、閉じるは doc-11 §7 に従う。**一覧列を持つ 2 区画とも同じ型を使う**（TASK-117。2026-08-05 の目視フィードバック由来）— どちらも「対象を選び、選んだ対象に対して操作する」区画（前項）で、そこへ対象を足す入口が 2 通りあると、利用者は区画ごとに足し方を覚え直すことになる。§6 が 3 カラム化で、decision-24 が写せなかったファイルの一覧で、それぞれ同じ判断をしている。**発行に成功しても層は閉じず、入力欄だけが空になる** — 作成は 1 件で終わるとは限らず（登録直後のプロジェクトへ文書をまとめて足す、マイルストーンを先に何本か切る）、閉じる形にすると入口 → 層 → 閉じるを件数ぶん繰り返すことになる。加えて閉じる形は `×` と「作成」を**下書きの行方の違う 2 つの出口**にし、doc-11 §7 が下部操作行を要求する条件に当たる。出口を 1 つに保てば、`×` を押したときに入力がどうなるかは破棄前確認が述べる（プロジェクト登録モーダルと同じ形。doc-11 §7）。**これは画面設計案 07 からの逸脱に数えない** — 07 の転記（doc-12 §8.3・§8.4）は作成が受け取る項目を挙げているだけで、それをどこに出すかを書いていない。書かれていないことから外れることはできないので、逸脱の数は §5 が 2 件・§6 が 3 件（画面全体で 5 件）のままである。
- **写せなかったファイルの一覧** … 一覧列の末尾に置き、写せなかったファイル（doc-4 §1）1 件を表示パスと失敗理由の 1 行として並べる領域を指す（decision-24）。**カードではない** — 選べず、右の列へ読み込まれない。id を持たないファイルは開く対象にも更新の宛先にもならないためで、カードと同じ見た目にすると押しても何も起きない領域が一覧に混ざる。**この領域は 0 件のとき出さない**（提供しない操作区画と同じ扱い。§9）。
- **提供しない操作区画** … v1.48.0 の Backlog CLI に対応手段が無い、doc-9 の照合が定まらない、または提供するかどうかが未決であるために Atlas が出さない操作を、名称・写像先・理由の 3 点で並べる区画を指す。押せないボタンを並べる代わりに置く。**理由は真であるものを書く**（CLI にできる操作を「CLI に手段が無い」と書かない。TASK-58 が `--tags ""` でこれを踏んだ）。**「CLI に対応手段が無い」ことは、この区画へ入れる十分な理由ではなくなった** — decision-21 の 3 条件を満たす操作は直接書き込み操作として提供し得るので、CLI に手段が無い操作をここへ入れるときは、その 3 条件のどれを満たさないかまで確かめる（マイルストーン説明の更新は満たしたのでここから出た。§6）。**列が 0 件になった区画は、区画ごと出さない**（§9）。

## 2. 位置づけ

本画面は、現行の台帳管理画面（TASK-39）とプロジェクト管理画面（TASK-40）を、**1 プロジェクト単位**へ組み替えたものである。両画面は「全プロジェクトの台帳」と「1 プロジェクトの文書・マイルストーン・新規タスク」という異なる粒度を並べており、利用者は同じプロジェクトを扱うのに 2 つの画面を行き来していた。本画面は粒度を 1 プロジェクトへ揃え、そのプロジェクトに対してできることを 1 箇所に集める。

- **入口**: スイムレーン（doc-7）のレーンヘッダ行にあるプロジェクト名、および行末の `›`。読取不能行（doc-7 §6）からも開ける（台帳エントリは読めるため、ルートの修正はここで行う）。
- **出口**: ヘッダの「← スイムレーン」と「このプロジェクトのレーンへ」。
- **画面が書くもの**: 概要区画は台帳ファイルだけを書く（doc-3 §2.1）。文書・マイルストーン・新規タスクの各区画は Backlog 更新アダプター（doc-5）を通じて対象プロジェクトの管理ファイルを書く。Atlas 自身は管理対象 Markdown を書かない（doc-2）。
- **全プロジェクトに効く設定**（表示テーマ・カード情報量ほか）は本画面に置かない。アプリ設定（decision-13）として固定ヘッダの設定から開く（doc-7 §2.1）。

## 3. 画面の構成

| 区画 | 内容 | 書き込み先 |
|---|---|---|
| ヘッダ | プロジェクト名・slug・タスク／文書／マイルストーンの件数・スイムレーンへの往復 | 書かない |
| 台帳読取専用帯 | 台帳ファイルが読み取り専用へ縮退している旨と理由（doc-3 §2.2） | 書かない |
| CLI 縮退帯 | 対応 CLI を検出できないため作成・更新を発行できない旨（doc-5 §5） | 書かない |
| 概要 | 台帳エントリ（project_root・backlog_root・Git remote 有無属性）と status 別名表、登録解除 | 台帳ファイル |
| 文書 | 文書一覧・作成・更新 | 管理ファイル（`doc create` / `doc update`） |
| マイルストーン | 一覧・選んだマイルストーンの操作・作成・提供しない操作区画 | 管理ファイル（`milestone add` / `rename` / `remove` / `archive`） |
| 新規タスク | 詳細な新規タスク作成 | 管理ファイル（`task create`） |

2 本の帯は互いに独立である。台帳読取専用は Atlas が書く先の問題、CLI 縮退は Backlog CLI が使えない問題で、片方だけが立つことがある。台帳操作は CLI を使わないため、CLI 縮退でも概要区画は動く。逆に台帳が読み取り専用でも、文書・マイルストーン・新規タスクは発行できる。帯はこの独立を利用者に伝わる形で書き分ける。

## 4. 概要区画（台帳エントリ）

### 4.1 編集できる属性とできない属性

doc-3 §3 の属性のうち、本区画が編集できるのは `project_root`・`backlog_root`・`status_aliases` と、Git remote 有無属性の再検出要求である。

- **slug は編集手段を提供しない**。slug は横断タスクID の左辺として全タスクの参照に使われ、変更は同一性を切る（doc-3 §3.1）。値の表示に添えて、変更するには登録を解除して登録し直すこと、そのとき履歴表示の同一性が切れることを述べる。押せない入力欄を置かない。
- **プロジェクトルートを変えたときは Backlog ルートも併せて送る**。既定は `<新ルート>/backlog` だが、送るのは欄に入っている値である。どちらを送るかを利用者が読み取れるよう、ルートを変えた時点でその旨を欄の直下に出す。移動が成立すると、そのプロジェクトについて開いている編集セッション（doc-8 §1）は閉じる。**「開いている編集セッション」は本項が数えず、doc-8 §1 の語が指すもの全部を指す** — 2026-08-07 まで実例は文書区画の 1 つだけだったが、§6 が編集セッションを持った時点で 2 つになり、実装は文書側しか閉じていなかった（PR #74 の 1R [P1]）。**ここに実例を書き並べないのは意図的で**、書き並べれば §6 が持った時点でこの項が偽になる（doc-11 §7 が被せ層の列挙で踏んだのと同じ形）。閉じるとき、その画面が旧ルートの読み取りに解決していた選択も一緒に落とす。
- **送る属性を明示する**: 変更した属性だけを送る。何を送るかを保存操作の直前に列挙し、変更が無いときは「変更なし」として保存を無効化する（無効化の表し方は doc-11 §5）。

### 4.2 status 別名表

status 別名表（doc-3 §3.3、decision-4）は「原文の status → 正準ステータス列」の表として編集する。1 行 = 原文の status 入力 + 正準 4 列からの選択。

各行には、その別名が実際に効くかどうかを添える。別名表は、対象プロジェクトが宣言している status に対してのみ効く（doc-3 §3.3）。宣言された集合にその status が無ければ、別名を書いてもそのタスクは未分類のままである。ただし `config.yml` が `statuses` を 1 つも宣言していないルートは別で、矛盾する宣言が無いため別名は効く（decision-4 が実測した未初期化ルート）。行ごとに次の 4 態を区別して示す。

- **宣言あり** … `config.yml` の `statuses` にある status。別名が効く。
- **draft 専用** … `config.yml` が宣言していないが、既知の draft 状態として扱う値（doc-4 §3.4）。別名が効く。
- **宣言集合なし** … `config.yml` が `statuses` を 1 つも宣言していないため、その status が宣言済みかを判定できない状態。矛盾する宣言が無いので別名は効く。
- **宣言なし → 効果なし** … 宣言された集合があり、そこに無い status。別名を書いてもタスクは未分類区画に残る。不整合の族の色で示し、書けてしまうが効かないことを見える状態にする。

**不整合の族の色を与えるのは 4 態目だけ**である。宣言集合なしでは別名が効くため、効かない別名と同じ印を付けると、直しようのないものを直せと言っていることになる。この 4 態は列対応規則の実装（`interpret::status` の `StatusDeclaration`）と 1 対 1 に対応する。

入力の検査は保存の前に行い、重複（大文字小文字・前後空白を同一と見なす）と、対応先が正準ステータス列でない行を拒否する。不正な別名を台帳から削除しない（doc-3 §3.3、TASK-42）。

### 4.3 登録解除

登録解除は台帳エントリを 1 件削除し、スイムレーンからレーンを 1 本外す。プロジェクト側のファイル（`backlog/` 以下のタスク・文書）は削除しない。

- 危険区画として他の操作と区画を分け、削除しないもの（プロジェクトのファイル）を明記する。
- 確認は slug の入力一致とする。一致するまで実行を無効化し、無効の理由を添える（doc-11 §5）。
- 台帳が読み取り専用のときは実行できない。理由は帯と操作の双方に出す。

## 5. 文書区画

- **配置は 3 カラム**（画面設計案 07、doc-12 §8.3）: 区画ナビ（12rem、§1）・文書一覧（16rem）・文書ペイン。文書一覧とは、頭に「文書 N 件」の見出しを持ち、文書ごとのカードを縦に並べる 16rem の列 — すなわち本区画の一覧列（§1）を指す。カードとは、文書一覧に 1 文書ぶんとして並ぶ押せる領域を指し、ID・type・title・タグ・未保存の印を持つ — 領域全体が選択の操作で、別の編集ボタンを持たず、選択中のカードは強調して示す（どの文書を開いているかは一覧から読めること。2026-08-05 の目視指摘）。**文書ペイン**とは、文書一覧の右で残り幅を取る列を指し、文書を選んでいない間は提供しない操作区画を、文書を選んでいる間は閲覧を、編集セッションが開いている間はその更新フォームだけを出す。**2026-08-07 に作成フォームがこの列から出た**（TASK-117。§1 の作成モーダルへ移った）ので、3 状態の 1 つ目にもう作成フォームは無い。「開いている編集の操作を作成フォームと混ぜない」（2026-08-05 の目視指摘）は、混ぜる相手が列から消えたことで満たされる — 退けた配置ではなく、その配置がもう作れないという状態になった。幅の値はいずれも content box を名指す（§1）。**2026-08-07 に編集ペインから改称した**（TASK-116）: 次項で選択が開くのは閲覧になり、この列が持つ 3 状態のうち編集は 1 つになったので、§6 が置いた「列の名はその列が開くもので決める」規則の下では編集ペインと呼べない。
- **選択は閲覧を開き、編集セッションを開かない — 2026-08-07 の改訂**（TASK-116。2026-08-05 の目視フィードバック由来）: 本節は 2026-08-07 まで「カードを押すことが選択で、選択が更新フォームを開く」と定めており、文書を 1 枚読むだけで編集セッション（doc-8 §1）が開いていた。**閲覧**とは、選んだ文書 1 件を編集セッションを開かずに読んでいる状態を指す。閲覧は入力欄を持たないので未保存入力を持ち得ず、閲覧だけをしている間は破棄前確認も起きない。**閲覧ヘッダ**とは、閲覧の先頭に置く領域を指し、title と「編集」を 1 行に並べ、その下に ID・type・tags・表示パスと、不整合のときだけ理由行（後述）を持つ（**2026-08-07 に「選択を解除」がこの行から抜けた**。TASK-121。次項の次）。ID を添えるのは、title が見出しに出た結果カードの ID と対応づける値が閲覧側に無くなるためで、カードが持つ 4 値（ID・type・title・タグ）はここに全部揃う。本文は閲覧ヘッダの下に、読み取った文字列そのままで出す — 整形しない（タスク詳細が Description に対して取っているのと同じ扱いで、Markdown を整形する手段はこの版のどこにも無い）。**本文には doc-8 §2.1 の行長上限 48rem を掛ける** — 文書ペインは残り幅を取る列なので、窓を広げるほど 1 行が長くなる。あの 48rem は「残り幅を取る列に置いた本文ブロック」そのものに対して測った値（TASK-113）であり、ここで新しい数を決める理由が無い。値は `placement.ts` の `PROSE_MAX_WIDTH_REM` を借り、2 つ目の 48 を置かない。**編集への切替**とは、閲覧ヘッダの「編集」の押下で、閲覧している文書について編集セッションが開く事象を指す。切替の後の未保存管理・保存・破棄前確認は従来のままで、変わったのは編集セッションが開く契機だけである。「編集を閉じる」が戻る先は閲覧であって、作成フォームではない。
- **文書を選んでいない間の文書ペインは、提供しない操作区画だけを出す**（2026-08-07 に決めた。TASK-117）。作成フォームが層へ移り、残ったのはこの区画 1 つである。**列を畳んだり 2 カラムへ落としたりはしない** — 選択のたびに列の本数が変わると、一覧のカードの幅も動く。**その上に「文書を選ぶと内容を表示します」の 1 行を置く** — 提供しない操作区画だけが載った列は、そこが何のための列なのかを述べておらず、読み手は「提供されていない何か」を探すことになる（§9 が 0 件の区画を出さない理由と同じ形の誤読である）。doc-11 §6 の `—` は使わない: あれは値の不在を述べる目印で、ここに無いのは値ではなく選択である。
- **選択を解除する操作は置かない — 2026-08-07 に撤去した**（TASK-121。同日の目視フィードバック由来。§6 と同じ形で、両区画から同時に抜いた）。本節は 2026-08-07 の午前まで、閲覧ヘッダに「選択を解除」を置き、押すと選択が無くなって文書ペインが非選択時の姿へ戻る、と定めていた。**置いた理由は 2 度書かれ、2 度とも当たらなくなった。**TASK-116 が置いたときの理由は、置かないと作成フォームに打った未保存入力がカードを選んだ時点で画面から消え、それでも画面を離れるときの破棄前確認（doc-8 §6.3）には数えられる、というものだった（同日のレビュー [P2]。§6 が TASK-64 の [P2] として名指しした失敗と同型）。**その失敗は TASK-117 が消した** — 作成フォームが作成モーダルへ移り、その入力は層と一緒に破棄されるので、画面外に残る入力そのものが無くなった。TASK-117 が書き直した 2 つ目の理由（どの対象も選んでいない状態は、一覧列を持つ区画がいつでも戻れる状態でなければならない）を、目視で不十分と判断したのが本改訂である。**残す理由を書き直せないなら撤去する、という本節自身の判断順序を、書き直した理由の側に適用した。**
- **撤去で失うのは「利用者の操作で非選択時へ戻ること」だけである**。非選択時の姿そのものは残る（前項）。閲覧が出しているのはカードが持つ 4 値と本文であって、非選択時にしか読めない値は 1 つも無いので、戻れないことで読めなくなるものは無い。**失うものの側から確かめる**のは TASK-116 が [P2] で踏んだ手順で、そのとき見落としたのは「戻れない」ではなく「入力を失う」の方だった。ここで失う入力は無い — 閲覧は入力を持たず、編集セッションの入力は選んでいる文書に結び付いて画面に出ており、作成モーダルの入力は層が自分で破棄する。
- **一覧の先頭を自動選択しない**。撤去しても非選択時は 3 経路で到達する（初回描画、再読み込みで選択対象が消えたとき、§6 では削除・アーカイブの成功）ので、自動選択を入れても非選択時の姿を描かずに済むわけではなく、状態は 1 つも減らない。加えて自動選択は、利用者がしていない選択を画面が主張することであり、選択対象が読み取りから消えたときに隣のカードへ飛ぶことになる — そこは「閲覧に出すものが無くなったので選択を落とす」を明示の理由として選択を落とすと決めた場所である（PR #65 の 1R [P2]）。
- **パスは一覧に出さず、表示パスとして文書ペインに出す — 画面設計案 07 からの意図的な逸脱の 2 件目**: 07 はカードにパスを描く（doc-12 §8.3）が、一覧は選ぶための最小情報に絞り、どのファイルかは選んだ側の列で述べる（2026-08-05 の目視指摘）。表示パスとは、読み取り層の `source_path` をプロジェクトルートからの相対へ直した表示専用の文字列を指す（ルートの下に無いパスはそのまま出す）。**閲覧ヘッダに出し、更新フォームでは path 欄の直上に繰り返す** — あちらは移動要求の欄であり、空欄＝移動しないという意味は現在の所在と並べて初めて読めるためである。更新フォームの path 欄が移動要求であり現在値を入れないことは変わらない。**2 か所とも現在の読み取りから導く**（導出は 1 か所で、2 か所がそれを引く）。編集セッションの baseline から引くと、隣のカードの ⚠️ が現在の読み取りから出ているのと食い違い、同じ 1 件について 2 つの読み取りが描くことになる（decision-22「導出は 1 回」。PR #71 の 1R [P2] が同じ形で出た）。
- **文書一覧と文書ペインは独立にスクロールする — 画面設計案 07 からの意図的な逸脱**: 07 は区画ナビの外側を単一スクローラにしており、列ごとの overflow を持たない（doc-12 §8）。しかし文書を選び直しても一覧の縦位置が保たれる（gmail 的な操作。TASK-63 の要求）には、一覧が自分のスクロールを持ち、文書ペインの内容の入れ替えがその位置に影響しないことが要る。ここは 07 に従わない。**文書ペインの中身が差し替わったときは、文書ペインだけを先頭へ戻す**。差し替わる事象は 4 つで、選択が成立して閲覧が開いたとき・編集への切替で更新フォームが開いたとき・編集セッションが閉じて閲覧へ戻るとき（「編集を閉じる」／更新の成功／破棄して続行）・**選択が落ちて非選択時の姿へ戻るとき**である。**4 つ目は 2026-08-07 に「選択を解除して」から書き換えた**（TASK-121）: 解除の操作が抜けた後も、再読み込みで選択対象が消えれば同じ差し替えが起きる。事象が入れ替わっただけで数は 4 のままである。**作成モーダルの開閉はこの 4 つに入らない** — 層はペインの上に乗るだけで、ペインが出しているものは変わらないためである（TASK-117）。**文書ペインが別のものを出すたびに戻す**のであって、列挙は事象の数え上げにすぎない — 事象を足したらここも足す。ペインの古いスクロール位置が残ると、新しく開いた側が表示範囲の上に隠れ、操作が効かなかったように見える。一覧の位置には触れない。
- **文書区画の破棄前確認は本節自身の契約である**: 未保存入力を持ったまま別の文書を選ぶ・編集を閉じる の 2 経路で確認し、問いは「文書の編集に未保存入力があります。（選んだ文書を開くと／編集を閉じると）破棄されます。」、答えは進む側「破棄して続行」・戻る側「入力に戻る」。doc-8 §6.3 の文言契約は借りない — あちらが定めるのはタスク詳細の編集セッションの 5 経路とモーダル（doc-11 §7 の被せ層）で、この確認はどちらにも属さない。問いは 2 列の外・上に出す — どちらの列がどこまでスクロールしていても見える位置に無いと、問いが立ったことに気づけない。**この 2 経路が確認を出すのは編集セッションが開いていて未保存入力があるときだけ**で、閲覧しかしていない状態から別のカードを選んでも確認は立たない（閲覧は未保存入力を持ち得ない）。経路の数も文言も TASK-116 は動かしていない。**TASK-117 も動かしていない** — 作成モーダルが持つ未保存入力の破棄前確認は doc-11 §7 の経路で、問いも答えもあちらのもの（doc-8 §6.3 の問い文＋「破棄して閉じる」「編集に戻る」）を使い、**本節の 2 という数はそこを数えていない**。同じ喪失を 2 通りに述べないための帰結で、本節が自分の文言を持つのはこの 2 経路についてだけである。**TASK-121 も動かしていない** — 撤去した選択の解除は閲覧からしか押せず、閲覧は未保存入力を持ち得ないので、この 2 経路のどちらでもなかった。**同じ撤去が §6 では経路を 1 つ落とす**（あちらは選択が入力欄を開いていたため）が、§6 は同じ改訂で「編集を閉じる」を得るので数は 2 のままである。**2 区画の 2 という数が同じ 2 経路を指すようになったのは本改訂からで**、それまでは片方が解除を数えていた。
- **一覧**: カード（ID・type・title・タグ・未保存の印）を並べ、カードを選ぶと文書ペインへ読み込んで閲覧を開く。パスは並べない（前項の逸脱のとおり、表示パスとして文書ペインに出す）。
- **選択中の強調と編集中チップは別々に立つ**（TASK-116）。強調は選択が指している 1 枚に付き、閲覧でも編集でも同じように付く。**編集中チップが付くのは編集セッションが開いているカードだけ**である — 選択と編集が別々になったので、強調は「いま読んでいる」を、チップは「いま編集している」を述べる。未保存の印は従来どおり編集セッションの未保存入力を述べる。
- **不整合な文書はカードに不整合印を 1 つ持つ**（decision-22 の ⚠️ をそのまま使う。decision-24 が対象を管理ファイル 1 件へ広げた）。**理由はアクセシブル名と `title` が全文で持ち、カードには語を出さない。**カードの ID・type・title・タグは判別できた値をそのまま出す — 写せなかったのは任意項目だけなので、残りは正常な文書と同じに読める（doc-4 §5）。
- **理由行は閲覧ヘッダに並べる**（decision-22 の理由行を借りる。decision-24 を TASK-116 が原地改訂した）。選んだ文書が不整合のときだけ出し、専用の区画は作らない。**decision-24 は「編集ペインの見出しの下」と定めていた** — 当時は選択が更新フォームを開いたので、それが選択で開く場所だった。選択が開く場所が閲覧になった以上、置き場も閲覧ヘッダへ移る。移さないと、不整合な文書を選んでも理由行が「編集」の向こうに隠れ、**doc-11 §2.4 の「ホバーだけが理由への唯一の経路になってはならない」に触れる**（PR #71 の 1R [P2] で 1 度直したのと同じ形である）。**更新フォームには繰り返さない** — 保証すべきなのは「理由を読むための場所が別にある」ことであり、選択が開く場所がその場所である以上、閲覧ヘッダ 1 つで足りる。**置くこと自体は doc-11 §2.4 の要求である** — 印グリフは「理由を読むための場所が別にある」ことを設計側が保証する条件で置いてよいものであり、タスクにおけるその場所が不整合区画（doc-8 §3）で、ここでは閲覧ヘッダがそれに当たる。
- **一覧の末尾に写せなかったファイルの一覧を置く**（§1、decision-24）。見出しの「文書 N 件」は**並んでいるカードの数のまま**とし、写せなかった件数を足さない — 足すと数と並びが食い違う。写せなかった件数はこの領域自身が述べる。**表示パスは §1 の一覧列に出さないと決めたパスとは別のものである** — カードのパスを省いたのは選ぶための最小情報に絞るためだが、写せなかったファイルは id を持たないので、パスがそれを名指す唯一の値である。
- **作成**: title（必須。`doc create` の位置引数）、type、docs 配下のパス。**一覧見出し行の作成の入口が上げる作成モーダルに出す**（§1。2026-08-07 に文書ペインから移した。TASK-117。2026-08-05 の目視フィードバック由来）。**選択の有無に関わらず届く** — 作成フォームがペインの非選択時の状態だったころは、1 件読んでいる途中に別の文書を足すには、当時あった選択の解除を押して読んでいたものを閉じるしかなかった。**その解除は撤去済みなので（上記）、作成が層にあることが選択の有無に関わらず届く唯一の根拠になっている。****本文は `doc create` では渡せない**（doc-5 §3 の create 写像は title・type・path のみ）ので、作成後に一覧でその文書を選び、閲覧ヘッダの「編集」から入れる。
- **更新**: title・type・tags・path・本文。触って値が変わった項目だけを 1 回の `doc update` で送り、何を送るかを保存の直前に列挙する。
  - **本文は全置換である**。v1.48.0 の `doc update --content` に部分更新の手段が無く、欄の全文をそのまま送る。この性質を保存前に明示する。
  - **path 欄は現在値を入れない**。読み取り層が持つのは走査で得た `source_path` であり、`-p` に渡す docs 相対パスとは別の値である。空欄＝移動しない、値を入れる＝移動要求、として扱う。
  - **タグ全消しを提供する**（TASK-109 の決定、2026-08-06）。**タグ全消し**とは、tags 欄からタグを 1 件も残さずに文書更新を発行し、その文書の frontmatter から tags を無くすことを指す（本節の初版が「tags を空にする操作」と呼び、提供しないとしていたもの）。写像へは**空集合の tags**として渡す。空集合の tags とは、更新操作の tags が要素 0 個であることを指し、タグ全消しの要求を表す — **未タッチの tags**（欄に触っていないので tags を送らない）とは別の値である。この 2 つを混ぜると、タグに触っていない更新が他人の付けたタグを消す。本節の更新が定める「触って値が変わった項目だけを送る」が、そのままこの区別である。
    提供する理由は 2 つある。①**`--tags ""` はタグを実際に消す**（v1.48.0 で終了コード 0、frontmatter から `tags:` キーごと消える。TASK-58 実測、TASK-109 で再実測）ので、提供しない理由に CLI の制約を挙げられない。本節の初版が挙げていた「効果が未確認で、同型の `--ref ""`・`--depends-on ""` は終了コード 0 のまま何も消さない」は、このフラグには当てはまらなかった。②2 件目までは外せて最後の 1 件だけを拒む形は、規則ではなく段差であり、利用者に述べられる理由を持たない。加えて**文書には外部エディタ経路が無い**（doc-8 §7 が定めるのはタスク詳細のもの）ので、提供しなければタグ全消しは Atlas の中に経路を持たず、管理ファイルを直接編集させることになる。タスクの参照・依存の全消しを外部エディタ経路へ送れるのとは事情が違う。
    **タグ全消しに告知は置かない**。「保存するとタグをすべて外します」の類を欄の隣に出す案は退けた — tags 欄は現在のタグを並べており、それが空になっているのは利用者が 1 件ずつ外した結果そのものなので、告知は読めば分かる状態の言い換えにしかならない（TASK-79 が落とすと定める型）。**path 欄と同列に扱わない**: あちらは常に空で現在値を持たないため空欄の意味が読めないが、tags 欄は現在値を出している。どの CLI フラグを発行するかも述べない — 利用者が確かめるのはタグが消えたことであって、`--tags ""` が走ったことではない。
    なおタグ値にカンマを含められないことは変わらない（CLI が 1 個のカンマ区切り値として受け取るため）。
- **削除は提供しない**。v1.48.0 の `doc` に delete/remove 相当のサブコマンドが無く、Atlas がファイルを直接消すことは decision-2 の境界の外にある。提供しない操作区画に理由とともに置く。

## 6. マイルストーン区画

- **配置は 3 カラム — 画面設計案 07 からの意図的な逸脱**: 区画ナビ（12rem、§1）・マイルストーン一覧（一覧列、§1）・マイルストーンペイン。07 のマイルストーン区画は一覧とフォームを縦に積んでおり、3 カラムではない（doc-12 §8）。それでも同じ形にするのは、本区画と文書区画（§5）の操作の性質が同じ — 対象を選び、選んだ対象に対して操作する — だからで、同じ性質のものを 2 通りに並べると、利用者は区画ごとに操作の仕方を覚え直すことになる。マイルストーン一覧とは、頭に「マイルストーン N 件」の見出しを持ち、マイルストーンごとのカードを縦に並べる一覧列を指す。カードとは、マイルストーン一覧に 1 マイルストーンぶんとして並ぶ押せる領域を指し、id・title・所属タスク件数を持つ — 領域全体が選択の操作で、操作ボタンを別に持たず、選択中のカードは強調して示す。**マイルストーンペイン**とは、マイルストーン一覧の右で残り幅を取る列を指し、マイルストーンを選んでいない間は「マイルストーンを選ぶと操作を表示します」の 1 行だけを、選んでいる間は閲覧を、編集セッションが開いている間はその編集入力だけを出す。**非選択時にこの列が空になったのは 2026-08-07 で**（TASK-117）、作成フォームが作成モーダル（§1）へ移り、提供しない操作区画は §9 のとおり 0 件で出さないため、載るものが 1 つも残らなかった。**それでも列は畳まない** — 理由は §5 と同じで、選択のたびに列の本数が変わると一覧のカードの幅も動く。**空の列をそのまま出さないのも §5 と同じ**で、1 行が「ここは選んだマイルストーンについて述べる列である」と述べる。**この列は 2026-08-07 に操作ペインから改称した**（TASK-121）: 本節は改称まで、開くのが改称・削除・アーカイブと説明の編集であって編集セッション（doc-8 §1）はどこにも現れない、という理由でこの列を操作ペインと呼んでいた。次項が選択の開く先を閲覧へ変えたので、§5 が置いた「列の名はその列が開くもので決める」規則の下ではもう操作ペインと呼べない。**§5 の文書ペインと同じ位置・同じ切替規則・同じ 3 状態の列になり、違いは並ぶ対象だけになった。**
- **選択は閲覧を開き、編集セッションを開かない — 2026-08-07 の改訂**（TASK-121。同日の目視フィードバック由来。§5 が TASK-116 で受けた改訂を、同じ形の一覧列を持つこの区画へ揃えたものである）: 本節は改訂まで「選択が改称・削除・アーカイブと説明の編集入力を同時に開く」と定めており、マイルストーンを 1 件読むだけで説明の入力欄が開いていた。**閲覧**とは、選んだマイルストーン 1 件を編集セッション（doc-8 §1）を開かずに読んでいる状態を指す（語は §5 のものをそのまま引く。区画ごとに定義し直さない）。閲覧は入力欄を持たないので未保存入力を持ち得ず、閲覧だけをしている間は破棄前確認も起きない — **§5 の閲覧が持つこの性質ごと引くのが、同じ語を使うということである。**閲覧が出すのは id・title・所属タスク件数と、不整合のときの理由行（後述）と、**説明の現在値**で、説明は読むだけの表示として、読み取ったまま（整形しない）出す。**§5 の閲覧が本文に対して取っている扱いをそのまま引く** — 空のときは「説明はありません。」と述べ、**本文には doc-8 §2.1 の行長上限 48rem を掛ける**（マイルストーンペインは残り幅を取る列なので、窓を広げるほど 1 行が長くなる。§5 が「残り幅を取る列に本文ブロックを置く画面は決め直さずに doc-8 §2.1 を引く」と定めたとおりで、ここが 2 例目である）。**閲覧ヘッダ**とは閲覧の先頭に置く領域を指し、title と「編集」を 1 行に並べ、その下に id・所属タスク件数を持つ。**編集への切替**とは、閲覧ヘッダの「編集」の押下で、閲覧しているマイルストーンについて編集セッションが開く事象を指す。**その向こうにあるのは説明の編集入力と改称・削除・アーカイブの 4 つで、入力を取る操作を閲覧の側に 1 つも残さない** — 残すと、この節の閲覧だけが未保存入力を持ち得る閲覧になり、同じ語が 2 節で違うものを指す。**アーカイブは入力を取らない**が、発行する操作を閲覧に残さないという同じ規則で編集の側に置く。
- **編集セッションは発行の成功でも閉じ、閲覧へ戻る**。閉じる契機は 4 つで、「編集を閉じる」・4 つの発行のいずれかの成功・選択が別のマイルストーンへ移ったとき・**プロジェクトルートの移動が成立したとき**である。**4 つ目は §4.1 が本節に依らず定めている** — あちらは「移動が成立すると、そのプロジェクトについて開いている編集セッション（doc-8 §1）は閉じる」と無条件に書いており、doc-8 §1 の語が本節のセッションを覆うようになった時点で、この節のセッションもその対象になった（PR #74 の 1R [P1]。**契機を数えた本節が、自分の外にある契機を数え落としていた**）。閉じないと、旧ルートの読み取りに対して打った説明や改称の入力が、id だけを頼りに移動先の同じ id へ発行される — マイルストーン id は `m-1`・`m-2` … なので文書 id より衝突しやすく、doc-9 §4 の照合は移動先自身の読み取りに対して通ってしまう。**選択も一緒に落ちる**（セッションを閉じた後、旧ルートの読み取りに解決した選択は指す先を持たない）。**発行の成功で閉じるのは §5 が文書更新について書いているのと同じ理由**で、発行後の baseline は既に存在しない版だからである。削除・アーカイブの成功では対象そのものが読み取りから消えるので、選択が落ちて非選択時の姿へ戻る（後述）。改称は v1.48.0 では id を変えない（doc-9 §4.2.1）ので選択は残り、閲覧へ戻る。
- **説明は一覧に出さず、マイルストーンペインに出す — 本節での逸脱の 2 件目**（本画面では 5 章の 2 件と合わせて 4 件目。数え直しを避けるため、以降も章ごとに数えたうえで画面全体の通し数を添える）: 一覧列は 16rem で、説明が読める幅ではない。一覧は選ぶための最小情報（id・title・所属タスク件数）に絞り、説明はそれを選んだ側の列で述べる（§5 が表示パスについて下したのと同じ判断）。**閲覧では読むだけの表示、編集セッションでは入力欄として出す**（前項）が、どちらもこの列の中であって、逸脱の中身は変わらない。
- **カードは編集中チップを持つ — 2026-08-07 の改訂**（TASK-121）。本節は改訂まで「編集中の印を持たない」と定め、その理由に「マイルストーン側には編集への切替が無く、選択がセッションを開くこともない」を挙げていた。**前々項がその理由を消したので、印も一緒に動く。**§5 と同じく**選択中の強調と編集中チップは別々に立つ**: 強調は選択が指している 1 枚に付いて「いま読んでいる」を述べ、チップは編集セッションが開いているカードだけに付いて「いま編集している」を述べる。**未保存入力の印は従来どおり持つ** — 開いている編集に入力があることは、マイルストーンペインがスクロールで見えていないときも一覧から読めること。
- **マイルストーン一覧とマイルストーンペインは独立にスクロールする**。理由は §5 と同じで、マイルストーンを選び直しても一覧の縦位置が保たれるには、一覧が自分のスクロールを持ち、ペインの内容の入れ替えがその位置に影響しないことが要る。**マイルストーンペインの中身が差し替わったときは、マイルストーンペインだけを先頭へ戻す。**差し替わる事象は §5 と同じ 4 つで、選択が成立して閲覧が開いたとき・編集への切替で編集入力が開いたとき・編集セッションが閉じて閲覧へ戻るとき（「編集を閉じる」／発行の成功／破棄して続行）・選択が落ちて非選択時の姿へ戻るときである（**2026-08-07 に 1 つから 4 つへ増えた**。TASK-121 が編集セッションを足したため）。**ペインが別のものを出すたびに戻す**のであって、列挙は事象の数え上げにすぎない。ペインの古いスクロール位置が残ると、新しく開いた側が表示範囲の上に隠れ、操作が効かなかったように見える。一覧の位置には触れない。
- **マイルストーン区画の破棄前確認は本節自身の契約である**: 編集セッションに未保存入力を持ったまま、**別のマイルストーンを選ぶ・編集を閉じる の 2 経路**で確認し、問いは「マイルストーンの編集に未保存入力があります。（選んだマイルストーンを開くと／編集を閉じると）破棄されます。」、答えは進む側「破棄して続行」・戻る側「入力に戻る」。doc-8 §6.3 の文言契約は借りない — あちらが定めるのはタスク詳細の編集セッションの 5 経路とモーダル（doc-11 §7 の被せ層）で、この確認はどちらにも属さない（§5 と同じ理由）。問いは 2 列の外・上に出す。ここで守る未保存入力とは、改称の新しい名称・削除の参照タスクの扱い・付け替え先・**説明の編集後の本文**として入力済みで、まだ発行していない値を指す。
  - **経路は 2026-08-07 に入れ替わったが、数は 2 のままである**（TASK-121。AC #4 が記録を求めているのはこの数である）。撤去した「選択を解除する」が 1 つ落ち、編集への切替が足した「編集を閉じる」が 1 つ入った。**入れ替えの後、§5 と §6 の 2 という数は同じ 2 経路を指す。**
  - **文言は 2 度動いた。**2026-08-06 に「マイルストーンの操作に」から「マイルストーンに」へ広げたのは、TASK-65 で説明の編集が加わり、守る対象が開いている操作の入力だけではなくなったためだった。2026-08-07 に「マイルストーンの編集に」へ戻したのは、**守る対象が全部 1 つの編集セッションの中に入った**ためである（TASK-121）。文言は守る対象を述べるものなので、対象が動いたら文言も動く — 広げたときと同じ規則を、狭まった側へ適用した。
  - **「操作を開いていないから未保存入力は無い」とは判定しない**のは変わらない。説明の入力欄は改称・削除・アーカイブのどれも開いていない間も編集セッションの中に出ているので、判定は開いている操作ではなく編集セッションの入力全体に対して行う。
- **一覧**: カード（id・title・所属タスク件数・未保存の印・編集中チップ）を並べ、カードを選ぶとマイルストーンペインへ読み込んで閲覧を開く。説明は並べない（本節 2 件目の逸脱のとおり、マイルストーンペインに出す）。
- **不整合なマイルストーンと写せなかったファイルの扱いは §5 と同じ**（decision-24）。カードは不整合印 ⚠️ を 1 つ持ち、理由はアクセシブル名と `title` が全文で持つ。**理由行の置き場は 2026-08-07 に §5 と揃った**（TASK-121）— 置き場を決める規則は初めから 2 区画とも「選択が開く場所の見出し下に置く」（decision-24）で同じで、分かれていたのは選択が開く場所の方だった。§5 は TASK-116 で閲覧になり、こちらは選択が開く場所が操作ペインのままだったので理由行もそこに残っていた。本節の選択が閲覧を開くようになったので、**理由行も閲覧ヘッダへ移る** — 移さないと、不整合なマイルストーンを選んでも理由行が「編集」の向こうに隠れ、doc-11 §2.4 の「ホバーだけが理由への唯一の経路になってはならない」に触れる。**規則は 1 度も変えていない。動いたのは規則が指す場所だけである**（decision-24 の実例をこの改訂で書き替えた）。**編集セッションには繰り返さない** — 保証すべきなのは「理由を読むための場所が別にある」ことで、選択が開く場所がその場所である以上、閲覧ヘッダ 1 つで足りる（§5 が更新フォームに繰り返さないのと同じ）。一覧の末尾に写せなかったファイルの一覧（§1）を置き、見出しの「マイルストーン N 件」は並んでいるカードの数のままとする。**ここで区画ごとに決め直さない** — 2 区画が同じ形の一覧列を持つ以上、そこへ足す領域も同じ規則で並ぶ（§9）。
- **マイルストーンペイン（閲覧）**: 閲覧ヘッダ（title と「編集」を 1 行、その下に id・所属タスク件数）、不整合のときの理由行、説明の現在値。入力欄は 1 つも無い。
- **マイルストーンペイン（編集セッション）**: 見出しに **id と「を編集」**、説明の編集入力と「説明を保存」、改称・削除・アーカイブ、「編集を閉じる」。**見出しが対象を名指すのは §5 の更新フォームと同じ形である** — 一覧のカードも強調と編集中チップで同じことを述べているが、2 列は独立にスクロールするので、選んだカードが表示範囲の外にある間もこの列だけで対象が読めなければならない（本節が未保存の印について述べているのと逆向きの、同じ理由）。**閲覧が出す残りの値はここに繰り返さない** — 編集を閉じれば同じ列に閲覧が戻る。
- **選択を解除する操作は置かない — 2026-08-07 に撤去した**（TASK-121。§5 と同時・同じ理由で、両区画から同時に抜いた。§5 の当該項に理由を書いてある）。**本節の側で撤去が動かすものは 2 つ**で、破棄前確認の経路が 1 つ落ちること（上記。同じ改訂が「編集を閉じる」を足すので数は 2 のまま）と、非選択時の姿へ戻る経路が利用者の操作から外れることである。**非選択時へは 3 経路で到達する**: 初回描画、再読み込みで選択対象が消えたとき、そして**削除・アーカイブの成功**（対象が読み取りから消えるため。本節だけが持つ 3 つ目である）。**一覧の先頭を自動選択しないのも §5 と同じ**で、理由もあちらに書いたものがそのまま当たる。
- **作成**: 名称（必須。`milestone add` の位置引数）と説明（`-d`）。**一覧見出し行の作成の入口が上げる作成モーダルに出す**（§1。2026-08-07 にマイルストーンペインから移した。TASK-117）。**§5 と同じ型にしたのは §1 が決めている** — 同じ形の一覧列を持つ 2 区画で、対象を足す入口を 2 通りにしない。ここで区画ごとに決め直さないのは、写せなかったファイルの一覧（decision-24）を 2 区画へ同じ形で置いたのと同じ判断である。
- **作成後の説明も編集できる — 本節での逸脱の 3 件目**（画面全体では 5 件目）。画面設計案 07 は「作成後の説明の編集は提供しない」と述べており（doc-12 §8.4）、本節は 2026-08-06 までそれに従っていた。外れる根拠は decision-21 で、v1.48.0 の `milestone` に update/edit が無いままでも、Atlas が**説明の本文範囲だけ**を直接書き込む（doc-5 §1 の直接書き込み操作）。**doc-12 §8.4 は転記なので直さない** — あちらは原文の値を写す文書であり、外れたことを記録するのはこちらである。
  - **編集の場所はマイルストーンペインの編集セッション**（**2026-08-07 に「操作ペイン」から改めた**。TASK-121 が編集への切替を入れ、入力を取る操作を閲覧の側に残さないと決めたため）。選んだマイルストーンの説明を複数行の入力欄として出し、明示保存で発行する。一覧のカードには出さない（本節 2 件目の逸脱のとおり）。**閲覧の側には現在値が読むだけの表示として出ている**ので、編集への切替を挟んでも説明が読めなくなる瞬間は無い。
  - **空にできる**（2026-08-06 にユーザーが確定）。CLI は `-d` 省略時に `Milestone: <name>` を置くので説明の無いマイルストーンを作らないが、利用者が書いた説明を消す手段が他に無く、消しても書き直せるので許す。文書のタグ全消し（§5）と同じ判断である。**空のときに画面が出す語は 2 か所で違う**（2026-08-07 に書き分けた。TASK-121）: 閲覧では「説明はありません。」と述べ（前掲。§5 の閲覧が空の本文に対して述べるのと同じ形）、編集セッションの入力欄では「説明なし」を placeholder として出す。**述べる文と、空欄が何であるかを示す語は別物である** — 前者は読んだ結果を述べる文、後者は打てる欄が空であることを示す語で、片方の語をもう片方へ流用すると、閲覧の 1 行が入力欄に見える。改訂前は編集入力しか無かったので後者だけがあった。
  - **行頭が `##` の行を含む説明は入力検査で拒み、理由を出す**。読み取り層は次の `##` で説明の本文範囲を切るため、そのまま許すと保存した文字列の一部が次の読み込みで画面から消える。§7 の「ラベルにカンマを含められない」と同じ型の入力検査で、**理由は「CLI に手段が無い」ではなく、読める範囲と書ける範囲を同じに保つため**である。
  - **`## Description` 見出しで開かれていない説明には書かない**。見出しも `SECTION:DESCRIPTION` 対も無いファイルと、説明が `SECTION:DESCRIPTION` 対で開かれているファイル（タスクファイルの書式で、マイルストーンでは手編集でしか現れない）の 2 つが該当し、どちらも CLI が書かない形を Atlas が作ることになる（decision-21 の条件 1）。発行を拒み、理由を出す。後者では説明が画面に出たまま保存だけが拒まれるが、これは decision-21 が用意している拒否である。
- **改称・削除・アーカイブを提供する**。doc-9 §4.2 が書き換え対象集合の決定規則と合否規則を定めたため、照合不能ではなくなった（TASK-45）。3 項目は提供しない操作区画から外れる。
  - **改称**: `milestone rename <from> <to>`。参照するタスクも併せて更新するかを選ばせる（外すと `--no-update-tasks`）。既定は更新する側とする。v1.48.0 の改称は id を変えないため、実際に書き換わるのは `milestone` 値が id 以外（title 直書きなど）のタスクだけである（doc-9 §4.2.1）。
  - **削除**: `--task-handling <clear|keep|reassign>` を必須の選択とし、`reassign` では `--reassign-to <milestone>` を必須入力として渡す。**v1.48.0 の削除はマイルストーンのファイルを消さず `archive/milestones/` へ移す**（doc-9 §4.2.1 実測）。画面は CLI の語に合わせて「削除」と呼びつつ、ファイルが残ることと、`keep` では参照するタスクが解決先の無い値を持ったままになることを述べる。
  - **アーカイブ**: `milestone archive <name>`。
- **実行前に書き換え対象集合を示す**。マイルストーンのファイルと、参照追随書き換えを伴う場合の参照タスク集合（doc-9 §4.2.2）を、件数と対象タスクで挙げてから実行させる。doc-9 §4.2.3 の合否規則が「利用者は何が書き換わるかを見て決める」ことを前提にしているため、集合を見せずに実行させる経路を置かない。
- **拒否は更新前競合として出す**。全件一致が崩れている場合、または未読タスクファイルがある場合、CLI は起動されずに拒否される（doc-9 §4.2.3）。版がずれた要素と未読タスクファイルをすべて挙げ、再読込を促す。照合不能の印（doc-11）はここでは使わない。版がずれているかを確かめた結果の拒否だからである。

提供しない操作区画は、名称・CLI 上の写像先・理由の 3 点を並べる。押せないボタンを並べない理由は、無効化されたボタンが「今は条件が揃っていないだけ」と読めるのに対し、ここに並ぶのは「Atlas がこの版で提供しない」ものだからである。

## 7. 新規タスク区画

`task create` を 1 回発行する。本区画が渡すのは title（必須・位置引数）・description・status・priority・milestone・labels・受入条件である。

- **CLI が受け取れる範囲より狭く絞るのは製品判断である**。v1.48.0 の `task create` は上記に加えて `-a`・`--plan`・`--notes`・`--ref`・`--depends-on` も受け取り、作成された管理ファイルへ保存する（doc-5 §3、2026-07-29 実測）。本区画がこれらを欄として出さないのは、CLI に手段が無いからではなく、Atlas の作成フォームを識別と分類に要る項目へ絞る判断である。画面にはこの理由を書き、「CLI に無い」とは書かない。
- **省いた項目に作成後の経路があるかは、項目ごとに違う**。実装計画・ノート・参照・依存はタスク詳細画面の編集（doc-8 §6）で足せる。**assignee は編集側で閉じた**（TASK-57 の決定）: `task edit -a` は CLI に在り、`TaskEdit` に assignee を持たせてタスク詳細の編集（doc-8 §6）から設定・変更する。本区画に欄を設けないのは、作成時にだけ設定できて後から変えられない経路を作らないためで、assignee にも「あとで足せる」が成り立つ。ただし解除だけは CLI に手段が無い（`-a ""` は沈黙無変更。doc-5 §3.1）。
- **ラベルにカンマを含められない**。CLI が 1 個のカンマ区切り値として受け取るため、1 件のつもりの値が 2 件に分かれる。入力検査で拒否し、理由を示す。
- **status の選択肢は宣言済みの原文 status とする**。`-s` は対象プロジェクトの `config.yml` が宣言する値だけを受け取り、未宣言の値は終了コード 1 で拒否される（doc-5 §3）。正準ステータス列名を選択肢に並べない。省略時は CLI の `default_status` に委ねる。スイムレーンの列内新規タスク入力（doc-7 §4.1）から来た場合は、その列の作成時 status 候補で確定済みとして扱う。両者が発行するのは同じ `task create` である。

## 8. 縮退時の表示

- **台帳読取専用**（doc-3 §2.2）: 概要区画の入力と登録解除を無効化し、理由を帯と各操作に出す。文書・マイルストーン・新規タスクは影響を受けない。
- **CLI 縮退**（doc-5 §5）: 文書・マイルストーン・新規タスクの発行を無効化し、理由を帯と各操作に出す。概要区画は影響を受けない。
- **ルート読取不能**（doc-4）: 文書・マイルストーン・新規タスクの一覧は出せない。概要区画は台帳エントリから描けるため出し、ルートの修正手段として機能させる。
- 無効化はすべて理由を伴う（doc-11 §5）。理由の無い無効化を置かない。

## 9. 後続への影響

- 本画面の実装は TASK-55。現行の台帳管理画面（TASK-39）とプロジェクト管理画面（TASK-40）を置き換える。
- **写せなかったファイルの一覧（§1）と、カードの不整合印は TASK-88（decision-24）で足した。**この形の一覧列を持つ区画を以降足すときは、§1 の一覧列と併せてこの領域も引く（§5・§6 の 2 か所で同じ規則が要ることは、6 章の 3 カラム化と同じ理由で証明済みである）。
- **選択が開くものは TASK-116 で閲覧になった（§5）。**右の列は編集ペインから**文書ペイン**へ改称し、理由行は編集ペインの見出し下から**閲覧ヘッダ**へ移した（decision-24 を原地改訂した）。**同じ形の一覧列を足す区画は、選択が何を開くかを先に決め、理由行をその場所に置く** — 置き場を「編集ペインの見出し下」のような特定の場所として引き写すと、選択が開くものが違う区画で doc-11 §2.4 の保証が切れる。マイルストーン区画（§6）が理由行を当時の操作ペインに残していたのは、同じ規則を適用した結果であって、§5 に従わなかったからではない。**TASK-117 は文書ペインから作成フォームを外した** — §5 の選択の解除が戻る先は非選択時の文書ペイン（提供しない操作区画と 1 行の案内）に決まり、**解除を置き続ける理由も §5 で書き直した**。
- **選択が開くものは TASK-121 でマイルストーン区画でも閲覧になった（§6）。**右の列は操作ペインから**マイルストーンペイン**へ改称し、理由行は操作ペインの見出し下から**閲覧ヘッダ**へ移り、カードは**編集中チップ**を持つようになった。**decision-24 の規則は 1 度も変わっていない** — 動いたのは規則が指す場所で、2 実例とも閲覧ヘッダになったのは 2 区画の選択が同じものを開くようになった結果である。**同じ形の一覧列を足す区画は、選択が何を開くかを先に決め、理由行・列の名・カードの印をその決定から導く**（3 つとも「選択が何を開くか」1 つに依存しており、独立に決めるものではない、というのが 2 区画で 2 度確かめられた形である）。
- **両区画から「選択を解除」が抜けた（TASK-121。§5・§6）。**TASK-116 が置いたときの理由（画面外の未保存入力）は作成フォームが層へ移った時点で消え、TASK-117 が書き直した 2 つ目の理由（いつでも非選択時へ戻れること）は目視で不十分と判断された。**非選択時の姿は残る** — 初回描画・再読み込みでの選択対象の消失・（§6 のみ）削除／アーカイブの成功 の 3 経路で到達するので、一覧の先頭を自動選択しても状態は減らない。**この形の一覧列を足す区画は、非選択時へ戻る導線を置くかどうかを、利用者の操作で戻れることではなく「戻らないと失う入力があるか」で決める** — 失う入力が無いなら導線は要らない、というのが 3 度書き直した末の形である。
- **対象を足す入口は TASK-117 で一覧見出し行の作成の入口と作成モーダルになった（§1）。**文書（§5）とマイルストーン（§6）の両方が同時に移り、**以降この形の一覧列を足す区画は §1 の 3 語をまとめて引く** — 一覧見出し行・作成の入口・作成モーダルは 1 組で、入口だけを置いて作成をペインに残すと、同じ一覧列を持つ区画で対象の足し方が 2 通りになる。**右の列の非選択時に何が残るかは区画ごとに違う**（§5 は提供しない操作区画 1 件、§6 は 0 件）が、**空になっても列は畳まず、その列が何のためのものかを述べる 1 行を置く**のは 2 区画で同じである。**被せ層はもう固定ヘッダの 3 入口に限らない** — doc-7 §2.1 が「本節は層を数えない」を、doc-11 §7 が被せ層の定義を列挙から層の形へ改めたのが、この変更に伴う 2 件である。**decision は書いていない**（doc 改訂だけで足りた例）。
- **意思決定（`decisions/`）には本画面に区画が無く、写せなかったファイルも不整合な意思決定も出す先が無い。**読み取り層は 3 種すべてを記録しており（doc-4 §5）、欠けているのは置き場だけである。**TASK-118（決定事項の閲覧）が一覧を作るとき、§5 と同じ形で写せなかったファイルの一覧を併せて置く。**正常な一覧が無いうちに写せなかったファイルだけを出さないのは、それを見た利用者が Atlas は意思決定を扱っていると読み、正常な意思決定を探して見つけられないためである（decision-24）。
- スイムレーン側の入口（レーンヘッダ行のプロジェクト名・`›`）と、固定ヘッダの「プロジェクトを登録」「設定」は doc-7 §2.1 が定める。
- マイルストーン区画の 3 カラム再構成（TASK-64）は済んだ。5 章が決めた構成 — 一覧が選択を保持する側の列、右の列がフォームと提供しない操作区画、両列の独立スクロール — を 6 章が引き、2 区画に共通する列を 1 章の一覧列として置いた。**以降、この形の列を足す区画は 1 章の一覧列を引く**（幅を各章で決め直さない）。**6 章が 5 章から意図して外していた 2 点は、2026-08-07 に 0 点になった**（TASK-121）: カードが「編集中」の印を持たないことと、右の列を文書ペインではなく操作ペインと呼ぶことで、**どちらも「こちらには編集への切替そのものが無いため」という 1 つの理由に載っていた**。6 章に編集への切替が入った時点で理由が消えたので、2 点とも 5 章と同じ形へ戻した。**1 つの理由に載せた例外は、その理由が消えると全部まとめて落ちる** — 逆に、例外ごとに別々の理由を書いていたら、消し忘れがどれか 1 つに残る形だった。
- マイルストーンの改称・削除・アーカイブは TASK-45 で有効化した。提供しない操作区画に残るのは **1 件**で、区画ごとの内訳は次のとおり。5 章（文書区画）は文書の削除（CLI に delete/remove が無く、ファイルそのものを消す操作なので decision-21 の直接書き込みにも入らない）の 1 件。**6 章（マイルストーン区画）は 0 件になり、区画そのものを出さない** — 作成後の説明の編集が TASK-65 で提供する側へ移ったためで、根拠は decision-21（`milestone update`/`edit` は v1.48.0 にも無いままだが、説明の本文範囲だけを Atlas が直接書く）。**タグ全消しは TASK-109 で外れた**（5 章）。区画の書き方（名称・写像先・理由の 3 点）は 6 章末が定め、5 章もそれに従う。**残る 1 件の理由は「CLI に手段が無い」で、未決を理由とする項目は残っていない**（1 章はその理由を認めているが、現に使っている項目は無い）。**この区画は 0 件になったら出さない** — 見出しだけが残ると、読み手は「何かが提供されていない」と読んで中身を探すことになる。
